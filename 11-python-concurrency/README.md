# 11 - Python Concurrency

A deep dive into the asyncio event loop's internals beyond day-to-day Scrapy usage: rescuing deliberately broken concurrent code, cancellation semantics, backpressure, GIL-related benchmarks to see where threads and processes actually help, and profiling live with py-spy.

Status: not generated yet (see GENERATION_STATE.md)
