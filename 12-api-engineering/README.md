# 12 - API Engineering

A FastAPI service built on top of module 02's (by then repaired) marketplace database: cursor-based pagination, caching strategy, and rate limiting, plus a dedicated security block covering SQL injection, authentication/authorization pitfalls, and secrets handling. The API surface here is also the target for module 19's type-safe SDK exercise.

Status: not generated yet (see GENERATION_STATE.md)
