# 10 - NoSQL Patterns

Redis used for things beyond caching: a rate limiter, distributed locks, deduplication, and Redis Streams. Paired with a direct comparison of MongoDB against Postgres JSONB for semi-structured data, to build judgment about when "just use JSONB" is the right call and when it isn't.

Status: not generated yet (see GENERATION_STATE.md)
