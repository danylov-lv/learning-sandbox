# NOTES

## Memory numbers observed

(fill in -- the actual `MEMORY USAGE` byte counts for the SET key and the
Bloom key after loading the same urls, and the ratio between them; whether
it matched what you expected before running the validator)

## False-positive rate observed

(fill in -- how many of the true unique urls the Bloom filter missed on the
first pass, as a fraction, and how that compared to the configured
`error_rate`)

## What I learned

(fill in after completing the task)

## Gotchas

(fill in after completing the task)

## Open questions

(fill in after completing the task)
