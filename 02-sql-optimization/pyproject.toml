[project]
name = "sql-optimization"
version = "0.1.0"
description = "Module 02: SQL optimization on the inherited Kupitron marketplace database"
requires-python = ">=3.12"
dependencies = [
    "psycopg[binary]==3.2.3",
    "numpy==2.1.3",
    "faker==30.8.2",
]
