# NOTES

## The envelope fields and what each means

(fill in -- op, before, after, source (ts_ms, lsn, txId), top-level ts_ms;
what's populated/null for each op type; what a tombstone looks like)

## How I decoded the Decimal price

(fill in -- the base64 -> signed int -> scale-shift chain, and why a plain
float would have been wrong)

## What I learned

(fill in after completing the task)

## Gotchas

(fill in after completing the task)

## Open questions

(fill in after completing the task)
