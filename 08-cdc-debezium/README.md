# 08 - CDC with Debezium

Change data capture end to end: Postgres to Debezium to Kafka to a downstream mart, including schema evolution as the source table changes shape and validation that the downstream mart actually converges with the source of truth. Reuses module 02's Postgres instance as the CDC source and module 07's redpanda as the streaming backbone.

Status: not generated yet (see GENERATION_STATE.md)
