# ci-meta

GitHub Actions CI for the sandbox itself: detects which module(s) changed in a given push/PR and runs only that module's tests, spinning up whatever service containers that module's docker-compose defines. Not a learning module in the mini-project sense — it's infrastructure for the repository as a whole.

Status: not generated yet (see GENERATION_STATE.md)
