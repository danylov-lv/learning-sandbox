# NOTES

## How I floored timestamps to their window

(fill in — the anchor, the window size, and the exact arithmetic used to go
from an `event_ts` string to a window-start datetime)

## What a naive offset-based window would have gotten wrong

(fill in — pick a specific late event if you can find one, and describe
which window it would land in by offset/arrival order vs which window it
actually belongs to by event_ts)

## What I learned

(fill in after completing the task)

## Gotchas

(fill in after completing the task)

## Open questions

(fill in after completing the task)
