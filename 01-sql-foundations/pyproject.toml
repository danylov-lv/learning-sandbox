[project]
name = "sql-foundations"
version = "0.1.0"
description = "Module 01: SQL foundations warm-up (price-intelligence scraping warehouse)"
requires-python = ">=3.12"
dependencies = [
    "psycopg[binary]==3.2.3",
    "numpy==2.1.3",
    "faker==30.8.2",
]
