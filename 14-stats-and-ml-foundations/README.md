# 14 - Stats and ML Foundations

Applied numpy/pandas and visualization work grounded in the price data scraped in earlier modules, followed by applied statistics on that same data, then a taste of sklearn and PyTorch. The goal is statistical and ML literacy sufficient to reason about a data pipeline's outputs, not to become an ML engineer.

Status: not generated yet (see GENERATION_STATE.md)
