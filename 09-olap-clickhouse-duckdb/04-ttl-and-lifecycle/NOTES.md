# NOTES

## Why the TTL didn't fire until I forced it

(fill in -- what you observed querying the count right after `load_from_raw`
vs. after `force_ttl`; why a MergeTree TTL is evaluated on merge and not on
insert or select; what `merge_with_ttl_timeout` implies about relying on a
background merge instead of forcing one)

## What I learned

(fill in after completing the task)

## Gotchas

(fill in after completing the task)

## Open questions

(fill in after completing the task)
