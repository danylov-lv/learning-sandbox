<!--
This is your deliverable. Fill in every section below for the
sample-project at ../sample-project. Delete this comment block when done.

Do not restate this template's instructions as your content — each
section body must be about the actual sample-project: its real test
command, its real conventions (see sample-project/README.md and
src/priceparser/__init__.py), and your own reasoning.
-->

## Commands

[fill in: the exact command(s) to run this project's tests, from which
directory, and anything else a fresh session needs to run before it can
usefully touch this code]

## Conventions

[fill in: the conventions this codebase actually follows — grounded in
what you read in sample-project/README.md and the source, not generic
Python advice]

## Architecture

[fill in: what's where, and why — the shape of this small project, so a
fresh session doesn't have to re-discover it by reading every file]

## What NOT to do

[fill in: concrete mistakes a fresh session (human or AI) could plausibly
make on this project, and the constraint that prevents each one]

## Memory vs rot

[fill in: your own reflection — what belongs in a CLAUDE.md for a project
like this, what belongs elsewhere (or nowhere), and why. Answer directly:
what would you leave OUT of this file, and what's the risk of putting it
in?]
