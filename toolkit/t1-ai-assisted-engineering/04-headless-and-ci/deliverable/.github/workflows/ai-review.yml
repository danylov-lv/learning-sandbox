# TODO: this workflow must trigger on a PULL REQUEST LABEL (not on every
# push), and run an AI review step using `claude -p` in headless mode
# when that specific label is applied. See the task README for the exact
# grading contract.
#
# This file currently only has a placeholder trigger and step so it stays
# valid YAML while unfinished -- replace both.

name: AI Review

on:
  push:
    branches: [main]

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: TODO
        run: echo "not implemented yet"
