---
name: "TODO: fill in -- must be exactly test-runner"
description: "TODO: one or two sentences Claude uses to decide WHEN to delegate to this agent, not just what it does"
tools: "TODO: comma-separated tool list, e.g. Read, Bash, Grep"
model: "TODO: e.g. sonnet, haiku, or inherit"
---

TODO: the system prompt for this agent. What should it actually DO when
invoked -- which command(s) does it run, how does it decide the scope of
what to test, what does it report back, and in what format? Reference
this repo's real verification convention (`uv run ...`, `PASSED` /
`NOT PASSED: <reason>`) rather than inventing a new one.
