# Hint 1

Run `uv build` inside `project/` right now, before changing anything, and
read the error. It's telling you exactly which table is missing before
uv can even figure out how to turn this directory into a wheel.
