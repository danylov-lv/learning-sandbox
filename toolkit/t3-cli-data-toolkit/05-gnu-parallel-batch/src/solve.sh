#!/usr/bin/env bash
# Run from the module root. Use `parallel --jobs <N> --joblog
# data/batch/joblog.txt ...` to process every data/batch/inputs/*.json
# file into data/batch/outputs/<same-filename> -- see ../README.md for
# the exact required output shape.
#
# TODO: replace this stub with the parallel invocation.

set -euo pipefail

echo "not implemented" >&2
exit 1
