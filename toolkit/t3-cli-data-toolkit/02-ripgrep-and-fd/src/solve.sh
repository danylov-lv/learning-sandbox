#!/usr/bin/env bash
# Print four labeled answer blocks to stdout -- see ../README.md for the
# exact required format and the definition of each question.
#
# TODO:
#   ===Q1=== -- distinct captured 5xx status codes under data/filetree/logs/
#   ===Q2=== -- *.config.json paths under data/filetree/, excluding vendor/
#   ===Q3=== -- count of `price` (not followed by `_usd`) matches under src/
#   ===Q4=== -- file count by extension across the whole tree

set -euo pipefail

echo "not implemented" >&2
exit 1
