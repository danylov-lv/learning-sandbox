// Challenge 04 — First and Last
//
// `First<T>` is the first element type of tuple `T`; `Last<T>` is the last.
// For an empty tuple both are `never`.

export type First<T extends readonly unknown[]> = unknown; // TODO: implement

export type Last<T extends readonly unknown[]> = unknown; // TODO: implement
