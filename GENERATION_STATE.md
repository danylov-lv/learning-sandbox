# GENERATION_STATE

Status legend: pending / in-progress / done

- [x] skeleton (root README, PROGRESS.md, CONVENTIONS.md, stub module READMEs) — done. Note: toolkit/ dirs and PROGRESS.md toolkit section are created during the toolkit module's own turn.
- [x] 01-sql-foundations — done. 10 tasks (01–09 + capstone 10 with 3 checkpoints), module README, PROGRESS section, .authoring notes; expected.json verified against live seeded DB, no reference solutions committed.
- [x] 02-sql-optimization — done. 14 tasks (01–13 + capstone 14 with 3 checkpoints and workload qc01–qc08), module README, PROGRESS section, .authoring notes (tasks-w1/w2/w3a/w3b/w3c/w4-capstone); all checkers verified against live seeded DB (stock fails cleanly, reference fixes pass in rolled-back txns; task 11 pass-path verified in scratch DB), no reference solutions committed, shared DB left byte-identical to stock.
- [x] 03-data-modeling — done. 4 tasks (01-relational-core q01–q04, 02-scd2-history q05–q08, 03-star-schema q09–q11 with mart-schema contract, 04-capstone-bitemporal q12/q13a/q13b/q14/q15 + DESIGN.md, checkpoints CP1–CP3), module README, PROGRESS section, .authoring/notes.md, harness/{common,events,ground_truth,validate}.py + questions.md. Data gitignored, regenerated deterministically (seed 42, 2,336,793 events); ground_truth.json cache keyed by data sha, all 16 keys with non-degeneracy assertions. validate.py verified live: fail-path graceful (0/16, exit 1, no tracebacks), compare machinery pass-path tested via throwaway scratch schema (removed after). No reference solutions committed. Stale empty dirs from an earlier 7-task draft removed.
- [x] 04-storage-and-formats — done. 8 tasks (01–07 + capstone 08 with CP1 pipeline/CP2 quality gates/CP3 DESIGN.md), module README, compose (MinIO, verified up), generate.py, harness/common.py, PROGRESS section, .authoring/notes.md. All validators verified live on the 400k test set: stock stubs fail cleanly (NOT PASSED, exit 1, no tracebacks), pass-paths proven via throwaway scratchpad reference impls (never committed). Mid-generation decontamination: tasks 03/04/08 had reference impls accidentally committed in src/ — replaced with NotImplementedError stubs, leftovers removed, re-verified. Stock state restored: bucket empty, data/ = raw + ground-truth.json only, MinIO down. Key empirics recorded in .authoring/notes.md (duckdb needs SET TimeZone='UTC' for probe reconciliation; delta-rs 1.6 metrics keys numFilesAdded/numFilesRemoved; MinIO storage_options incl. AWS_S3_ALLOW_UNSAFE_RENAME).
- [x] 05-distributed-processing-spark — done. 8 tasks (01-lazy-plans-and-explain, 02-partitions-and-shuffles, 03-joins-broadcast-vs-smj, 04-udfs-and-arrow, 05-windows-at-scale, 06-parquet-to-minio-s3a, 07-polars-calibration host-side, capstone 08-scrape-lake with CP1 silver lake / CP2 shuffle tuning / CP3 DESIGN.md), module README, compose (spark 3.5.3 + MinIO 9501/9502, ports recorded in CONVENTIONS.md), generate.py (seed 50505, 2M authoring set committed as data/, gitignored), harness/common.py incl. get_plan/plan_has, PROGRESS section, .authoring/notes.md. All validators verified live: stock stubs fail cleanly (NOT PASSED, exit 1), pass-paths proven via throwaway reference impls in gitignored scratch/ (deleted; stubs diff-verified restored). Task 01's check_notes_filled bumped to min_chars=650 (template alone scored 441). Stock restored: bucket price-lake-05 empty, data/ = raw-events + reference + ground-truth.json, no results-local.json/__pycache__, root probe_*.py folded into .authoring/notes.md and deleted. Key empirics in .authoring/notes.md (AQE vs partition counts, session-config leakage, row_number matches GT top-n, PartitionFilters gate, polars ComputeError on malformed NDJSON, plan node names doubled in formatted mode).
- [x] 06-pipelines-and-orchestration — done. 10 tasks (01-first-dag-raw-to-staging, 02-incremental-idempotent-loads, 03-backfill-and-recovery, 04-poison-records-and-alerting, 05-contract-gate-pandera, 06-contract-evolution, 07-spark-stage-silver-lake, 08-dbt-marts-over-oltp, 09-prefect-migration, capstone 10-capstone-end-to-end with CP1 build+backfill / CP2 failure drills / CP3 DESIGN.md) + optional k8s-bonus (hand-written Helm chart), module README, PROGRESS section, CONVENTIONS ports (Postgres 54306, Airflow UI 8306, MinIO 9601/9602). Stack: Airflow 3.1 + warehouse Postgres 16 + MinIO + alert-sink via docker-compose, verified healthy. Data: generate.py seed 60606, 14 days 2025-06-01..14, gitignored except committed ground-truth.json. .authoring: design, notes-infra-{data,docker}, notes-tasks-{01-03,05-06}, notes-live-verification. First session to run the stack live: all 11 validators fail cleanly on stock (NOT PASSED, exit 1, no crash); pass-path proven live via throwaway reference DAGs (deleted) for tasks 01-04. Three bugs found+fixed: (1, blocking 05/06/capstone) core UNIQUE(source_site,product_url,scraped_at) collapsed distinct valid records sharing a same-second key while GT valid_records counts byte-distinct payloads, so core_count<valid_records was unsatisfiable — fixed in generate.py with a deterministic zero-RNG-draw scraped_at nudge among valid records, proven GT-invariant (ground-truth.json sha b04769..303e unchanged) with per-day distinct-key==valid_records for all 14 days and determinism preserved; the rest of a correct 05/06 solution was verified live (locale parser exact to 0.00, quarantine bounds, seller_rating, contract_drift alerts, downstream_check.sql). (2) missing host ./logs broke the bind mount and aborted every `airflow dags test` — shipped logs/.gitkeep. (3) ops.quarantine.raw_line disagreed between task 04 (text, canonical) and 05 (integer) — 05 fixed to text. Stock restored: docker compose down -v (volumes wiped), data/ = 14 days + ground-truth.json, alerts emptied, dags/ = .gitkeep+smoke_env only, no scratch/zz_ref committed. DEFERRED (stock-fail verified but full live pass-path NOT built this session): 07 Spark, 08 dbt (needs module 02 stack up), 09 Prefect, 10 capstone CP1-3, k8s-bonus cluster half — 05/06 rest on the data-level natural-key proof plus the live everything-but-count verification.
- [x] 07-streaming — done. 10 tasks (01-log-vs-queue-and-offsets, 02-delivery-semantics, 03-consumer-groups-rebalancing, 04-exactly-once-into-postgres, 05-windowed-aggregation, 06-lag-monitoring, 07-compacted-topics, 08-kafka-transactions-eos, 09-rmq-vs-kafka-writeup, capstone 10-capstone-streaming-pipeline with CP1 steady / CP2 chaos-consistency / CP3 DESIGN.md) + optional k8s-bonus (consumer Deployment + HPA + PDB, empty-templates+.gitkeep like module 06). Stack: redpanda v24.3.5 (Kafka API 19092, admin 19644, Console 8307) + Postgres 16 (54307, schemas core/ops/mart) via docker-compose, verified healthy. Data: generate.py seed 70707, ~200k price-update events over a 2h event-time window with ~2% late events; data/ gitignored except committed ground-truth.json. Shared harness/common.py (kafka/pg helpers incl. end_offsets/committed_offsets/consumer_lag, produce_events with transactional support, drain, guarded validators). .authoring/design.md is the spoiler contract; added .authoring/notes-live-verification.md this session. Module README + CONVENTIONS ports were already in place; PROGRESS 07 section filled this session. Split across two sessions: prior session generated infra + tasks 01-05,07,09 and verified 04/07 pass-paths (scratch-t04/t07); THIS session generated 06/08/10/k8s-bonus via subagents and verified live. All validators fail cleanly on stock stubs (NOT PASSED, exit 1, only last output line surfaced — no traceback leak, via a _last_line() helper). Pass-paths proven live via throwaway reference impls in gitignored scratch-*/ dirs (all deleted; stubs diff-verified restored): t06 (snapshot lag 0 then 100000 across 6 partitions, alert fired), t08 (read_committed drain seq-set == {0..199999} exactly after a mid-txn crash = topic-to-topic EOS), t10 CP1/CP2/CP3 all PASSED (CP2: exact ground-truth match after injected crash + two-instance rebalance + final run; CP3: DESIGN.md + CP1/CP2 re-run), k8s-bonus (helm template+lint renders Deployment/HPA/PDB). Orchestrator review fixes: (1) t10 CP1 timeout 360->900s and CP2 rebalance/final 420/300->900/600s, and reframed the pipeline.py perf note + hint-3 from "the timeout will fire" to throughput guidance — a correct naive per-message solution must pass on the exact ground-truth match (structural check), not be gated on a fixed wall-clock (per SPEC timing-varies-by-machine rule); the CTE + `synchronous_commit off` fast path stays as optional advice. Dependency added: pyyaml>=6.0.2 to pyproject.toml (needed by k8s-bonus validator's YAML parsing; uv.lock synced). Stock restored: leftover scratch-t04/scratch-t07 from the prior session deleted; no scratch dirs, no reference solutions, no DESIGN.md tracked; every src/ scaffold retains its NotImplementedError. Stack left running (docker compose up) — bring down with `docker compose down -v` if a byte-identical stock state is needed.
- [x] 08-cdc-debezium — done. 8 tasks (01-connector-setup-snapshot-vs-streaming, 02-change-event-anatomy, 03-updates-and-deletes-downstream, 04-schema-evolution, 05-replica-lag-and-alerting, 06-exactly-once-materialization, 07-cdc-vs-rescraping-writeup, 08-capstone-converge with CP1 steady replica / CP2 chaos convergence / CP3 DESIGN.md), module README, PROGRESS section, CONVENTIONS ports (SANDBOX_08_*: source PG 54308, mart PG 54318, Kafka 19093, redpanda admin 19645, console 8308, Connect REST 8383). Stack: source Postgres 16 wal_level=logical -> debezium/connect:3.0.0.Final on Kafka Connect -> redpanda v24.3.5 -> mart Postgres 16, via docker-compose, verified healthy. Data: generate.py seed 80808, seeds SOURCE directly via COPY (5000 products / 20000 offers, offers_price_sum 1884008.69); no events file (snapshot phase IS the initial-state capture); data/ gitignored except committed ground-truth.json; build_workload() exports deterministic insert/update/delete bursts validators reuse. Shared harness/common.py (kafka + pg + Kafka Connect REST helpers, decode_value/change_op envelope decoders, replication-slot introspection; guarded validators, _last_line() so no traceback leaks). Converter: JsonConverter schemas-enabled; NUMERIC prices arrive as base64 Kafka Connect Decimal (decimal.handling.mode left at precise default) — decoding it is the point of task 02. REPLICA IDENTITY FULL on both tables so before-images are real. .authoring/{design.md spoiler contract, notes-infra.md, notes-live-verification.md}. Three waves across sessions: (1) infra+harness+generate.py live-verified via a throwaway s08-probe connector; (2) parallel task authoring; (3) THIS session's serial live verification. All validators fail cleanly on stock stubs (NOT PASSED, exit 1). Pass-paths proven live via throwaway reference impls written in place then reverted byte-identical (never committed): t01 snapshot 20000+5000 op=r then c/u/d streaming with populated before-image; t02 tally exact + all 300 prices decoded exactly from base64 Decimal; t03 replica converged (20200) after 500/1000/300 burst; t04 converged across ADD COLUMN discount_pct; t05 lag 0/alert FALSE then lag 3300/alert TRUE with slot_lag_bytes under a burst; t06 exact (20400) after two crashes, applied_changes==drained non-tombstone count; t07 writeup sections pass; t08 CP1 mart==source (applied_changes 22700), CP2 converged incl discount_pct after two crashes + mid-stream schema change + two bursts (applied_changes 22145, lag snapshot recorded), CP3 design-doc + CP1/CP2 re-run path. Two problems found+fixed: (1) t08 src/pipeline.py + src/monitor.py were inherited FULLY IMPLEMENTED (a prior wave left solutions in, docstrings said "TODO" but bodies held real code, no NotImplementedError) — converted both to proper stubs matching the module convention, impls verified correct then reverted so no reference solution is committed. (2) validate_cp2.py CRASH_2_AFTER=20000 was unreachable (resumed run 2 has only ~13.4k messages, per-run counter restarts at 0, so the second injected crash never fires and the run exits 0) — the identical bug task 06 already fixed; lowered to 10000 (mirrors task 06's CRASH_AFTER_2=10000) and re-ran fixed CP2 to PASSED against the reference impl; CP3 imports validate_cp2.main() so the fix covers it too. Stock restored: all 8 src stubs back to raise NotImplementedError (t02 has two), t07 ANSWER.md/NOTES.md back to templates, no DESIGN.md tracked, source reseeded (20000 offers, 5000 products, no discount_pct), no connectors/slots/s08.* topics. Stack left running — docker compose down -v for a cold stock state.
- [x] 09-olap-clickhouse-duckdb — done. 9 tasks (01-mergetree-and-primary-index, 02-materialized-views, 03-replacingmergetree-dedup, 04-ttl-and-lifecycle, 05-postgres-vs-clickhouse-50m, 06-duckdb-on-parquet, 07-duckdb-vs-clickhouse, 08-when-clickhouse-when-duckdb writeup, capstone 09-capstone with CP1 ClickHouse serving layer / CP2 DuckDB cross-check / CP3 DESIGN.md), module README, PROGRESS section, CONVENTIONS ports (SANDBOX_09_CH_HTTP_PORT 8309, CH native 9309, PG 54309). Stack: clickhouse/clickhouse-server:24.8 (LTS) + postgres:16 via docker-compose, both healthy. Data: generate.py seed 90909, one denormalized fact table `observations` (50M rows at SCALE=1.0) materialized into three sinks — Postgres (row store, PK-only index-light), ClickHouse observations_raw (MergeTree ORDER BY (category, product_id, scraped_at)), and a Hive-partitioned Parquet lake (category=<x>/); data/ gitignored except committed data/ground-truth.json (numpy-computed answer key). generate.py rewrites GT to whatever SCALE it runs; GROUND_TRUTH_ONLY=1 restores the 50M key fast. Shared harness/common.py (ch/pg/duckdb clients, `ch_read_rows` pruning probe over system.query_log, time_it/read_baseline/write_baseline, guarded validators printing PASSED/`NOT PASSED`). build_duplicate_batch(seed,n) in generate.py = the pure-numpy dedup fixture for task 03. .authoring/{design.md spoiler contract, notes-infra.md}. Authored this session by 9 parallel Sonnet subagents (8 tasks + capstone) against a live stack loaded at SCALE=0.01 (500k rows); GT temporarily set to 0.01 for verification then restored to 50M. All validators fail cleanly on stock stubs (NOT PASSED, exit 1, no traceback via guarded). Pass-paths proven live per task via throwaway reference impls written in place then reverted byte-identical (never committed) + all t0N_* ClickHouse objects dropped: t01 pruning (full sum(price) read 500000 rows vs pruned 8192 = one granule); t02 SummingMergeTree MV, 1440 (day,category) keys matched GT exactly after 5 incremental insert batches (7199 partial rows required FINAL/GROUP BY to collapse); t03 RMT+FINAL, 6000 raw rows -> 1915 distinct keys, survivors correct per key; t04 TTL DELETE 15 MONTH retained 231392/500000 (cutoff 2025-04-11), forcing genuinely required; t05/t07 correctness matched GT on both engines (t07 also cross-engine agreement), timing kept INFORMATIONAL not asserted (at 500k ClickHouse HTTP/per-query overhead makes it slower than Postgres and the DuckDB-vs-CH winner flips run-to-run — the real gap is a 50M-scale phenomenon, documented in the READMEs); t06 DuckDB-on-Parquet matched GT + pruned to one partition file; t08 writeup validator gates on filled ANSWER.md (5 sections, min 250 chars, no placeholder, grounding keywords); t09 CP1 rollup+business-questions matched GT (price_sum 36,780,355.61 at 0.01, top sellers, per_category_instock), CP2 DuckDB reproduced all independently + pruning, CP3 filled DESIGN.md + re-runs CP1/CP2 as subprocesses. Two ClickHouse gotchas found+baked into task 03 (and reused by capstone): (1) `optimize_on_insert` (session default 1) pre-collapses duplicate-key rows within a single insert block, silently defeating the dedup premise — insert with `settings={"optimize_on_insert": 0}`; (2) clickhouse-connect converts naive Python datetimes using the CLIENT machine's local tz, not the server's UTC — validators that insert naive datetimes set `os.environ.setdefault("TZ","UTC")` before first import. No reference solutions committed anywhere (grep-verified: only `raise NotImplementedError` + docstring guidance in every src/); ANSWER.md/DESIGN.md left as unfilled templates. Stock restored: GT back at 50M, ClickHouse `SHOW TABLES` = only observations_raw, no scratch/baseline-local.json/__pycache__ tracked. Stack left running — `docker compose down -v` for a cold stock state.
- [x] 10-nosql-patterns — done. 8 tasks (01-rate-limiter, 02-distributed-lock, 03-dedup-filter, 04-redis-streams-consumer, 05-mongodb-document-modeling, 06-mongodb-vs-jsonb, 07-nosql-decision-writeup, capstone 08-capstone with CP1 steady two-consumer materialization / CP2 chaos crash+XAUTOCLAIM convergence / CP3 DESIGN.md), module README, PROGRESS section, CONVENTIONS ports (SANDBOX_10_*: Redis Stack 6310, MongoDB 27310, Postgres 54310). Stack: redis/redis-stack-server:7.4.0-v3 (bundles RedisBloom BF.*, needed by task 03) + mongo:7 (root sandbox/sandbox, authSource=admin, db `sandbox`) + postgres:16 (task 06 JSONB side only), via docker-compose, all healthy in seconds. Data: generate.py two numpy streams (products seed 10101, events seed 10102) writes data/{products.json,events.json} NDJSON gitignored + data/ground-truth.json committed; SCALE=1.0 = 20000 products / 25000 events; events COUPLED to the catalog (each scrapes a real product, price = catalog price * exp(N(0,0.08)), 70% introduce a new product without replacement / 30% re-scrape a hot one => exactly 30% duplicate urls, 17500 unique); build_products/build_events are pure so validators can synth the corpus in-memory. Namespacing keeps the shared stack collision-free (Redis keys `s10:tNN:`, Mongo collections `tNN_`, Postgres schema `t06`) so validators run in parallel — never FLUSHALL. Shared harness/common.py (redis/mongo/pg client factories all lazy, guarded/passed/not_passed, redis_flush_prefix SCAN+DEL, run_concurrently). .authoring/{design.md spoiler contract, notes-infra.md wave-1 build, notes-live-verification.md this session}. Split across sessions: wave-1 built infra + generate.py (live-verified: 3 services healthy, RedisBloom BF.* probed, generate.py deterministic sha, GT numbers pinned) + authored all 8 tasks; THIS final session verified every task live via one parallel subagent each. All 8 stock stubs fail cleanly (single-line NOT PASSED, exit 1, no traceback — @guarded holds). Pass-paths proven live via throwaway reference impls written in place then reverted byte-identical (never committed): t01 rate limiter (50/500 admitted under concurrency, isolation, window reset; Lua EVAL INCR+PEXPIRE); t02 lock (mutual exclusion 120/120, wrong-owner reject, monotonic fences; SET NX PX + Lua compare-and-del + INCR fence); t03 dedup (SET exact 17500 vs Bloom 14 FP/0 FN at 1.6% memory; RedisBloom confirmed on the live image); t04 streams (2000 at-least-once, 500 stranded PEL reclaimed by 2nd consumer via XAUTOCLAIM); t05 Mongo modeling (correctness vs GT incl graded_query.count=2276, nested_query.count=2052, index-backed IXSCAN — validator explains raw filters so COLLSCAN can't pass); t06 Mongo-vs-JSONB (containment 2276 + nested black 2052 match on BOTH engines, PG uses gin(jsonb_path_ops) not Seq Scan, Mongo IXSCAN, partial update; 11 stubbed functions); t07 writeup (5 exact `##` sections >=200 chars, no `[fill in`, >=6/8 grounding keywords, NOTES.md >=150 chars); t08 capstone CP1 count=17500 price_sum=1700991.53 == GT, CP2 survived crash+XAUTOCLAIM reclaim (5000 reclaimed, XPENDING 0) still exact, CP3 DESIGN.md 5 sections + CP1/CP2 re-run green (ref: one doc per product_id in t08_state, per-batch max-(scraped_at,event_id) winner, watermark-guarded idempotent bulk_write). No bugs found (no wall-clock timeout gates a correct impl; CP2 timing is structural XAUTOCLAIM idle knobs, the 300/600s in validate.py are outer subprocess wrappers). This session also CREATED the missing 08-capstone/README.md, filled PROGRESS, bumped root README est-evenings 4->5–6, and fixed a stale "ten functions" -> "eleven" line in task 06's README. No reference solutions committed anywhere (grep-verified: NotImplementedError in every src/ stub, ANSWER.md/DESIGN.md unfilled templates); no scratch dirs, no *-local.json, no task-dir __pycache__ tracked (all gitignored). Stack left running — docker compose down -v for a cold stock state.
- [x] 11-python-concurrency — done. 9 tasks (01-event-loop-and-blocking, 02-taskgroup-structured-concurrency, 03-cancellation-and-timeouts, 04-backpressure-bounded-queue, 05-semaphore-rate-limiting, 06-gil-decision-matrix, 07-sync-async-bridging, 08-profiling-py-spy, capstone 09-capstone-async-scraper with CP1 steady / CP2 chaos / CP3 design memo), module README, PROGRESS section, root README already listed it (5-6 evenings). PURE PYTHON — no docker, no CONVENTIONS ports entry: `harness/peer.py` runs an in-process aiohttp mock server bound to 127.0.0.1:0 (ephemeral, no collisions), dial-in latency/jitter/max_concurrency/rate/error knobs + a PeerStats(total_requests, max_observed_concurrency, error_responses, throttled); the concurrency gate rejects BEFORE incrementing in_flight so max_observed_concurrency <= cap is a hard invariant. Data: generate.py seed 111111, pure numpy `build_corpus` -> 3000 pages `/p/{i}` -> {product_id, category, price} (Zipf categories, lognormal prices); data/corpus.json gitignored, data/ground-truth.json committed (sha ab2a3e98..5343: n_pages=3000, price_sum=324536.21, per_category_count electronics 1316 / home-goods 616 / kitchen 389 / toys 279 / sporting-goods 202 / apparel 198), regenerates deterministically. Shared harness/common.py (not_passed/passed/guarded/_last_line, time_it, write_baseline/read_baseline for machine-local *-local.json, load_ground_truth, run_async, snapshot_tasks/leaked_tasks, measure_peak_memory via tracemalloc, rss_bytes). Verification philosophy per design.md: structural/behavioral by default (leaked_tasks==[], max_observed_concurrency<=cap, aggregate vs ground truth with float tol on money); memory-bounded via tracemalloc PEAK RATIO not absolute bytes (task 04); relative-timing-only via baseline.py + read_baseline (tasks 06 GIL matrix + 08 profiling — the only two places the module makes speed claims, never an absolute wall-clock number). Toolchain uv (aiohttp, numpy, psutil, py-spy, pytest-asyncio). Generated across two waves: wave-1 built infra + harness + generate.py + tasks 01-05,07 (partial); THIS final wave generated 06/08/09 and completed 01/04/07 via 6 parallel Sonnet subagents, then a 7th serial subagent fixed+verified the peer/timing-sensitive tasks. ALL 11 validators fail cleanly on stock stubs (single-line NOT PASSED, exit 1, zero traceback lines — @guarded holds). Pass-paths proven live via throwaway reference impls in gitignored scratch/ then reverted byte-identical (never committed): t01 offloaded gather+to_thread (heartbeat frac ~1.0 vs inline ~0.54); t04 bounded Queue (peak-mem ratio 0.99x vs unbounded 3.92x, ~2.2x threshold discriminates); t05 semaphore+rate-limiter (throttled=0); t06 measured process 2.60x vs thread 0.94x for CPU-bound (GIL) + ~7.8x concurrent I/O, asserted margins have headroom; t07 bounded to_thread input-order-preserving (inline reference fails the responsiveness check); t08 py-spy attached cleanly on this Windows host (no elevation, caught compute_signature at top of active+gil stack), validator gates on behavioral heartbeat + ANSWER content (CONCURRENCY=1 to keep the fixed-vs-broken signal separated); t09 CP1 count=3000 price_sum=324536.21 max_observed_concurrency=24 == ground truth, CP2 converges to identical aggregate under error_rate=0.2+jitter with cap held/no leaks, CP3 gates DESIGN.md + re-runs CP1/CP2 (600s outer safety timeouts, never a correctness gate on wall-clock). TWO Windows/Proactor bugs found+fixed this wave (see .authoring/notes-live-verification.md): (A) mock_peer's IocpProactor.accept-loop Task false-flagged as leaked by any before/after-within-peer leak check — fixed VALIDATOR-side (no harness change) by snapshotting tasks BEFORE entering mock_peer and checking leaked_tasks AFTER teardown (spans peer lifetime); Linux/selector never hits this since accept is a callback not a Task, so prior modules missed it — applied to tasks 01/05, capstone already had it. (B) task 01's heartbeat check couldn't discriminate offloaded from inline because pure-Python CPU-bound work offloaded to threads still contends on the GIL (offloaded==inline ticks) AND the premise was false (that's task 06's lesson) — reframed the blocking cost to GIL-RELEASING (time.sleep stand-in = blocking I/O / native parser), recalibrated check 3 to this machine's achievable solo heartbeat rate (Windows ~64Hz floor makes elapsed/interval unreachable) asserting frac>=0.75; learner-facing "CPU-bound" wording retargeted to "blocking (I/O / GIL-releasing native)". Incidental: task 05 src/fetcher.py shipped a leftover broken full impl (raised ExceptionGroup) not a stub — restored to raise NotImplementedError. No reference solutions committed anywhere (grep-verified: raise NotImplementedError in every learner src/ except intentional non-stubs 06/src/workloads.py provided + 08/src/app.py the broken app to profile); ANSWER.md/DESIGN.md unfilled templates; no scratch/*-local.json/__pycache__ tracked. .authoring: design.md (spoiler contract), notes-infra.md (wave-1), notes-task08.md, notes-live-verification.md (this wave). Pre-existing unrelated working-tree edit `01-sql-foundations/03-currency-normalized-revenue/src/query.sql` left uncommitted (not part of this module).
- [x] 12-api-engineering — done. 10 tasks (01-pagination-offset-vs-cursor, 02-response-caching-redis, 03-rate-limiting-and-quotas, 04-background-jobs-and-idempotency, 05-streaming-large-exports, 06-sql-injection, 07-auth-jwt-and-refresh, 08-secrets-management, 09-load-test-and-bottleneck-hunt, capstone 10-capstone-catalog-api with CP1 steady / CP2 chaos-hardening / CP3 DESIGN.md), module README, PROGRESS section, CONVENTIONS ports (SANDBOX_12_PG_PORT 54312, SANDBOX_12_REDIS_PORT 6312), root README row corrected (est 5-6 -> 6-7; the old one-liner wrongly claimed "FastAPI on the module 02 DB"). OWN stack, NOT module 02's (design.md's "Own stack" section: module 02's DB is deliberately wrecked as ITS curriculum; reusing it would contaminate every pagination/caching benchmark here) — postgres:16 + redis:7 via docker-compose. No fixed API port anywhere: harness/service.py always binds 127.0.0.1:0, so parallel task/validator runs never collide. Data: generate.py seed 121212, one clean properly-indexed `shop` schema (2000 sellers / 60 categories / 200000 products / 20000 users / 500000 orders / 1188048 order_items, ~259MB, ~60s); data/ gitignored except committed ground-truth.json (sha d96d3ab6..a305, reproduced byte-identical this session); SCALE shrinks it, GROUND_TRUTH_ONLY=1 = fast answer-key path. Namespacing keeps the shared stack parallel-safe: `shop` is READ-ONLY (no task writes to it), writable state goes in per-task schemas tNN, Redis keys `s12:tNN:` flushed via redis_flush_prefix (NEVER FLUSHALL). Shared harness/{common,service,load}.py (guarded/passed/not_passed, pg/redis helpers, fixture-password scrypt contract, run_app/run_app_subprocess/asgi_client, bombard() asyncio load generator — no locust). .authoring/{design.md spoiler contract, notes-infra.md wave-1, notes-live-verification.md this wave}. Three waves: (1) infra+harness+generate.py; (2) tasks 01-06 authored, never committed, never reviewed; (3) THIS session restored/completed 01-06, authored 07-10 via 5 parallel Sonnet subagents, verified all live, wrote docs. ALL 12 validators fail cleanly on stock (exit 1, exactly ONE `NOT PASSED:` line, zero traceback leak). Pass-paths proven live via throwaway refs (scratch-*/ or in-place) then reverted byte-identical — none committed. TWO CONTAMINATION BUGS inherited from wave 2 and fixed this session: (1) task 04's src/app.py shipped a WORKING reference solution for get_or_create_job (the real stub was parked beside it in an untracked src/app.py.stub-backup) — a naive `grep -c NotImplementedError` missed it because the file held 4 stubs AND 1 solution; restore lesson: count stubs against the EXPECTED per-file number, not just nonzero. (2) task 06 shipped the FULLY SOLVED security task — both fix layers already applied (parametrized `ILIKE %s` + a `_search_dsn()` connecting as the least-privilege t06_search role the learner is meant to create), self-identifying as `# THROWAWAY REFERENCE FIX ... never committed`; caught by RUNNING tests/exploit.py, which reported `EXPLOIT FAILED: payload returned HTTP 500` — i.e. the security contract's "exploit must SUCCEED against stock" direction was broken. Restored to vulnerable stock (f-string interpolation + pg_dsn() admin conn); leftover t06_search ROLE dropped from live PG (schema t06 is legitimately created by validate.py's own setup, so it stays). Both were on-disk only — module 12 had never been committed, so git history was never contaminated. Security contract proven BOTH directions: 06's exploit leaks user4242@kupitron-mail.example + its scrypt hash out of shop.users via UNION against stock, and fails after the reference fix. Task 07: 9/9 traps rejected (alg=none, HS256/RS256 confusion, tampered sig, absent sig, expired, refresh-as-access, access-as-refresh, rotated-then-reused refresh, cross-user IDOR) + a NEGATIVE CONTROL (narrowing /me's catch to ExpiredSignatureError flipped 4/9 traps to VULNERABLE) proving the battery isn't rubber-stamping. Task 08: 6/6 planted secrets found incl. the history-only one, 0/4 decoys; fixture deterministic (identical manifests + commit shas across runs). Task 09: measured stock ~35 rps / p95 ~880-930ms vs full fix ~242-271 rps / p95 118-141ms (~7.0-7.6x); thresholds MIN_RPS_RATIO=4.5 / MAX_P95_RATIO=0.5 chosen FROM measurement so a full fix has headroom on slower machines but an N+1-only fix (4.1x / p95 0.68x) correctly FAILS; anti-cheat confirmed (hardcoded seller_name fails the oracle before any throughput check). Capstone: CP1/CP2/CP3 all PASSED against the reference, CP3 shown re-running CP1+CP2 as subprocesses; chaos drills proven non-vacuous (rate limiter admitted EXACTLY 10/50 then 10/100 concurrent — atomicity actually exercised; Redis-down asserts the specific X-Cache: BYPASS header, not a coincidental 200). Empirics in notes-live-verification.md: pyjwt 2.13.0 blocks PEM-shaped HMAC keys in encode() so traps.py hand-builds the confusion JWT; InvalidKeyError is a PyJWTError but NOT an InvalidTokenError; two access tokens minted in the same wall-clock second are legitimately byte-identical (assert the REFRESH token rotates, not the access token); harness redis_client()/pg_conn() sys.exit() on failure so app code needing graceful degradation must use its own low-level client; run_app_subprocess needs env PYTHONPATH on Windows and is the clean way to fake "Redis down" without stopping the shared container; Windows git marks loose objects read-only (rmtree needs onerror chmod); git commits are cumulative snapshots so task 08's fixture exposes valid_commits rather than one exact sha. Task 09's docstrings originally enumerated all three planted defects (would have handed over the hunt) — rewritten to plain code, mechanisms live only in the hint tiers. Task 07 commits a FIXTURE RSA keypair, clearly labeled "NEVER commit a real private key; this one signs nothing but fixture data" (same posture as fixture passwords; deliberate tension with task 08's lesson, resolved by labeling). Stub-handler style intentionally differs: 01/04/07/10 register NotImplementedError -> clean 501; 02/03/05 let it 500 and say so in their own READMEs. No reference solutions committed anywhere (grep-verified; only intentional non-stubs are 06's vulnerable app and 09's slow app); DESIGN.md + NOTES.md ship unfilled; no scratch/leaky-repo/*-local.json/__pycache__ tracked (.gitignore gained leaky-repo/ and 08-secrets-management/service/secrets/). Stack left running — docker compose down -v for a cold stock state. Pre-existing unrelated working-tree edit 01-sql-foundations/03-currency-normalized-revenue/src/query.sql predates this session, left untouched.
- [ ] 13-scraping-at-scale — pending (stub README only)
- [ ] 14-stats-and-ml-foundations — pending (stub README only)
- [ ] 15-llm-in-pipelines — pending (stub README only)
- [ ] 16-testing-engineering — pending (stub README only)
- [ ] 17-system-design — pending (stub README only)
- [ ] 18-rust-track — pending (stub README only)
- [ ] 19-ts-track — pending (stub README only)
- [ ] 20-kubernetes — pending (stub README only)
- [ ] toolkit (t1–t4) — pending (directory not created yet)
- [ ] ci-meta — pending (stub README only)

Rules: one module at a time, in this order. Update this file after every module. Skeleton (README.md, PROGRESS.md) is generated first, before module 01.
