# GENERATION_STATE

Status legend: pending / in-progress / done

- [x] skeleton (root README, PROGRESS.md, CONVENTIONS.md, stub module READMEs) — done. Note: toolkit/ dirs and PROGRESS.md toolkit section are created during the toolkit module's own turn.
- [x] 01-sql-foundations — done. 10 tasks (01–09 + capstone 10 with 3 checkpoints), module README, PROGRESS section, .authoring notes; expected.json verified against live seeded DB, no reference solutions committed.
- [x] 02-sql-optimization — done. 14 tasks (01–13 + capstone 14 with 3 checkpoints and workload qc01–qc08), module README, PROGRESS section, .authoring notes (tasks-w1/w2/w3a/w3b/w3c/w4-capstone); all checkers verified against live seeded DB (stock fails cleanly, reference fixes pass in rolled-back txns; task 11 pass-path verified in scratch DB), no reference solutions committed, shared DB left byte-identical to stock.
- [x] 03-data-modeling — done. 4 tasks (01-relational-core q01–q04, 02-scd2-history q05–q08, 03-star-schema q09–q11 with mart-schema contract, 04-capstone-bitemporal q12/q13a/q13b/q14/q15 + DESIGN.md, checkpoints CP1–CP3), module README, PROGRESS section, .authoring/notes.md, harness/{common,events,ground_truth,validate}.py + questions.md. Data gitignored, regenerated deterministically (seed 42, 2,336,793 events); ground_truth.json cache keyed by data sha, all 16 keys with non-degeneracy assertions. validate.py verified live: fail-path graceful (0/16, exit 1, no tracebacks), compare machinery pass-path tested via throwaway scratch schema (removed after). No reference solutions committed. Stale empty dirs from an earlier 7-task draft removed.
- [x] 04-storage-and-formats — done. 8 tasks (01–07 + capstone 08 with CP1 pipeline/CP2 quality gates/CP3 DESIGN.md), module README, compose (MinIO, verified up), generate.py, harness/common.py, PROGRESS section, .authoring/notes.md. All validators verified live on the 400k test set: stock stubs fail cleanly (NOT PASSED, exit 1, no tracebacks), pass-paths proven via throwaway scratchpad reference impls (never committed). Mid-generation decontamination: tasks 03/04/08 had reference impls accidentally committed in src/ — replaced with NotImplementedError stubs, leftovers removed, re-verified. Stock state restored: bucket empty, data/ = raw + ground-truth.json only, MinIO down. Key empirics recorded in .authoring/notes.md (duckdb needs SET TimeZone='UTC' for probe reconciliation; delta-rs 1.6 metrics keys numFilesAdded/numFilesRemoved; MinIO storage_options incl. AWS_S3_ALLOW_UNSAFE_RENAME).
- [x] 05-distributed-processing-spark — done. 8 tasks (01-lazy-plans-and-explain, 02-partitions-and-shuffles, 03-joins-broadcast-vs-smj, 04-udfs-and-arrow, 05-windows-at-scale, 06-parquet-to-minio-s3a, 07-polars-calibration host-side, capstone 08-scrape-lake with CP1 silver lake / CP2 shuffle tuning / CP3 DESIGN.md), module README, compose (spark 3.5.3 + MinIO 9501/9502, ports recorded in CONVENTIONS.md), generate.py (seed 50505, 2M authoring set committed as data/, gitignored), harness/common.py incl. get_plan/plan_has, PROGRESS section, .authoring/notes.md. All validators verified live: stock stubs fail cleanly (NOT PASSED, exit 1), pass-paths proven via throwaway reference impls in gitignored scratch/ (deleted; stubs diff-verified restored). Task 01's check_notes_filled bumped to min_chars=650 (template alone scored 441). Stock restored: bucket price-lake-05 empty, data/ = raw-events + reference + ground-truth.json, no results-local.json/__pycache__, root probe_*.py folded into .authoring/notes.md and deleted. Key empirics in .authoring/notes.md (AQE vs partition counts, session-config leakage, row_number matches GT top-n, PartitionFilters gate, polars ComputeError on malformed NDJSON, plan node names doubled in formatted mode).
- [x] 06-pipelines-and-orchestration — done. 10 tasks (01-first-dag-raw-to-staging, 02-incremental-idempotent-loads, 03-backfill-and-recovery, 04-poison-records-and-alerting, 05-contract-gate-pandera, 06-contract-evolution, 07-spark-stage-silver-lake, 08-dbt-marts-over-oltp, 09-prefect-migration, capstone 10-capstone-end-to-end with CP1 build+backfill / CP2 failure drills / CP3 DESIGN.md) + optional k8s-bonus (hand-written Helm chart), module README, PROGRESS section, CONVENTIONS ports (Postgres 54306, Airflow UI 8306, MinIO 9601/9602). Stack: Airflow 3.1 + warehouse Postgres 16 + MinIO + alert-sink via docker-compose, verified healthy. Data: generate.py seed 60606, 14 days 2025-06-01..14, gitignored except committed ground-truth.json. .authoring: design, notes-infra-{data,docker}, notes-tasks-{01-03,05-06}, notes-live-verification. First session to run the stack live: all 11 validators fail cleanly on stock (NOT PASSED, exit 1, no crash); pass-path proven live via throwaway reference DAGs (deleted) for tasks 01-04. Three bugs found+fixed: (1, blocking 05/06/capstone) core UNIQUE(source_site,product_url,scraped_at) collapsed distinct valid records sharing a same-second key while GT valid_records counts byte-distinct payloads, so core_count<valid_records was unsatisfiable — fixed in generate.py with a deterministic zero-RNG-draw scraped_at nudge among valid records, proven GT-invariant (ground-truth.json sha b04769..303e unchanged) with per-day distinct-key==valid_records for all 14 days and determinism preserved; the rest of a correct 05/06 solution was verified live (locale parser exact to 0.00, quarantine bounds, seller_rating, contract_drift alerts, downstream_check.sql). (2) missing host ./logs broke the bind mount and aborted every `airflow dags test` — shipped logs/.gitkeep. (3) ops.quarantine.raw_line disagreed between task 04 (text, canonical) and 05 (integer) — 05 fixed to text. Stock restored: docker compose down -v (volumes wiped), data/ = 14 days + ground-truth.json, alerts emptied, dags/ = .gitkeep+smoke_env only, no scratch/zz_ref committed. DEFERRED (stock-fail verified but full live pass-path NOT built this session): 07 Spark, 08 dbt (needs module 02 stack up), 09 Prefect, 10 capstone CP1-3, k8s-bonus cluster half — 05/06 rest on the data-level natural-key proof plus the live everything-but-count verification.
- [x] 07-streaming — done. 10 tasks (01-log-vs-queue-and-offsets, 02-delivery-semantics, 03-consumer-groups-rebalancing, 04-exactly-once-into-postgres, 05-windowed-aggregation, 06-lag-monitoring, 07-compacted-topics, 08-kafka-transactions-eos, 09-rmq-vs-kafka-writeup, capstone 10-capstone-streaming-pipeline with CP1 steady / CP2 chaos-consistency / CP3 DESIGN.md) + optional k8s-bonus (consumer Deployment + HPA + PDB, empty-templates+.gitkeep like module 06). Stack: redpanda v24.3.5 (Kafka API 19092, admin 19644, Console 8307) + Postgres 16 (54307, schemas core/ops/mart) via docker-compose, verified healthy. Data: generate.py seed 70707, ~200k price-update events over a 2h event-time window with ~2% late events; data/ gitignored except committed ground-truth.json. Shared harness/common.py (kafka/pg helpers incl. end_offsets/committed_offsets/consumer_lag, produce_events with transactional support, drain, guarded validators). .authoring/design.md is the spoiler contract; added .authoring/notes-live-verification.md this session. Module README + CONVENTIONS ports were already in place; PROGRESS 07 section filled this session. Split across two sessions: prior session generated infra + tasks 01-05,07,09 and verified 04/07 pass-paths (scratch-t04/t07); THIS session generated 06/08/10/k8s-bonus via subagents and verified live. All validators fail cleanly on stock stubs (NOT PASSED, exit 1, only last output line surfaced — no traceback leak, via a _last_line() helper). Pass-paths proven live via throwaway reference impls in gitignored scratch-*/ dirs (all deleted; stubs diff-verified restored): t06 (snapshot lag 0 then 100000 across 6 partitions, alert fired), t08 (read_committed drain seq-set == {0..199999} exactly after a mid-txn crash = topic-to-topic EOS), t10 CP1/CP2/CP3 all PASSED (CP2: exact ground-truth match after injected crash + two-instance rebalance + final run; CP3: DESIGN.md + CP1/CP2 re-run), k8s-bonus (helm template+lint renders Deployment/HPA/PDB). Orchestrator review fixes: (1) t10 CP1 timeout 360->900s and CP2 rebalance/final 420/300->900/600s, and reframed the pipeline.py perf note + hint-3 from "the timeout will fire" to throughput guidance — a correct naive per-message solution must pass on the exact ground-truth match (structural check), not be gated on a fixed wall-clock (per SPEC timing-varies-by-machine rule); the CTE + `synchronous_commit off` fast path stays as optional advice. Dependency added: pyyaml>=6.0.2 to pyproject.toml (needed by k8s-bonus validator's YAML parsing; uv.lock synced). Stock restored: leftover scratch-t04/scratch-t07 from the prior session deleted; no scratch dirs, no reference solutions, no DESIGN.md tracked; every src/ scaffold retains its NotImplementedError. Stack left running (docker compose up) — bring down with `docker compose down -v` if a byte-identical stock state is needed.
- [x] 08-cdc-debezium — done. 8 tasks (01-connector-setup-snapshot-vs-streaming, 02-change-event-anatomy, 03-updates-and-deletes-downstream, 04-schema-evolution, 05-replica-lag-and-alerting, 06-exactly-once-materialization, 07-cdc-vs-rescraping-writeup, 08-capstone-converge with CP1 steady replica / CP2 chaos convergence / CP3 DESIGN.md), module README, PROGRESS section, CONVENTIONS ports (SANDBOX_08_*: source PG 54308, mart PG 54318, Kafka 19093, redpanda admin 19645, console 8308, Connect REST 8383). Stack: source Postgres 16 wal_level=logical -> debezium/connect:3.0.0.Final on Kafka Connect -> redpanda v24.3.5 -> mart Postgres 16, via docker-compose, verified healthy. Data: generate.py seed 80808, seeds SOURCE directly via COPY (5000 products / 20000 offers, offers_price_sum 1884008.69); no events file (snapshot phase IS the initial-state capture); data/ gitignored except committed ground-truth.json; build_workload() exports deterministic insert/update/delete bursts validators reuse. Shared harness/common.py (kafka + pg + Kafka Connect REST helpers, decode_value/change_op envelope decoders, replication-slot introspection; guarded validators, _last_line() so no traceback leaks). Converter: JsonConverter schemas-enabled; NUMERIC prices arrive as base64 Kafka Connect Decimal (decimal.handling.mode left at precise default) — decoding it is the point of task 02. REPLICA IDENTITY FULL on both tables so before-images are real. .authoring/{design.md spoiler contract, notes-infra.md, notes-live-verification.md}. Three waves across sessions: (1) infra+harness+generate.py live-verified via a throwaway s08-probe connector; (2) parallel task authoring; (3) THIS session's serial live verification. All validators fail cleanly on stock stubs (NOT PASSED, exit 1). Pass-paths proven live via throwaway reference impls written in place then reverted byte-identical (never committed): t01 snapshot 20000+5000 op=r then c/u/d streaming with populated before-image; t02 tally exact + all 300 prices decoded exactly from base64 Decimal; t03 replica converged (20200) after 500/1000/300 burst; t04 converged across ADD COLUMN discount_pct; t05 lag 0/alert FALSE then lag 3300/alert TRUE with slot_lag_bytes under a burst; t06 exact (20400) after two crashes, applied_changes==drained non-tombstone count; t07 writeup sections pass; t08 CP1 mart==source (applied_changes 22700), CP2 converged incl discount_pct after two crashes + mid-stream schema change + two bursts (applied_changes 22145, lag snapshot recorded), CP3 design-doc + CP1/CP2 re-run path. Two problems found+fixed: (1) t08 src/pipeline.py + src/monitor.py were inherited FULLY IMPLEMENTED (a prior wave left solutions in, docstrings said "TODO" but bodies held real code, no NotImplementedError) — converted both to proper stubs matching the module convention, impls verified correct then reverted so no reference solution is committed. (2) validate_cp2.py CRASH_2_AFTER=20000 was unreachable (resumed run 2 has only ~13.4k messages, per-run counter restarts at 0, so the second injected crash never fires and the run exits 0) — the identical bug task 06 already fixed; lowered to 10000 (mirrors task 06's CRASH_AFTER_2=10000) and re-ran fixed CP2 to PASSED against the reference impl; CP3 imports validate_cp2.main() so the fix covers it too. Stock restored: all 8 src stubs back to raise NotImplementedError (t02 has two), t07 ANSWER.md/NOTES.md back to templates, no DESIGN.md tracked, source reseeded (20000 offers, 5000 products, no discount_pct), no connectors/slots/s08.* topics. Stack left running — docker compose down -v for a cold stock state.
- [x] 09-olap-clickhouse-duckdb — done. 9 tasks (01-mergetree-and-primary-index, 02-materialized-views, 03-replacingmergetree-dedup, 04-ttl-and-lifecycle, 05-postgres-vs-clickhouse-50m, 06-duckdb-on-parquet, 07-duckdb-vs-clickhouse, 08-when-clickhouse-when-duckdb writeup, capstone 09-capstone with CP1 ClickHouse serving layer / CP2 DuckDB cross-check / CP3 DESIGN.md), module README, PROGRESS section, CONVENTIONS ports (SANDBOX_09_CH_HTTP_PORT 8309, CH native 9309, PG 54309). Stack: clickhouse/clickhouse-server:24.8 (LTS) + postgres:16 via docker-compose, both healthy. Data: generate.py seed 90909, one denormalized fact table `observations` (50M rows at SCALE=1.0) materialized into three sinks — Postgres (row store, PK-only index-light), ClickHouse observations_raw (MergeTree ORDER BY (category, product_id, scraped_at)), and a Hive-partitioned Parquet lake (category=<x>/); data/ gitignored except committed data/ground-truth.json (numpy-computed answer key). generate.py rewrites GT to whatever SCALE it runs; GROUND_TRUTH_ONLY=1 restores the 50M key fast. Shared harness/common.py (ch/pg/duckdb clients, `ch_read_rows` pruning probe over system.query_log, time_it/read_baseline/write_baseline, guarded validators printing PASSED/`NOT PASSED`). build_duplicate_batch(seed,n) in generate.py = the pure-numpy dedup fixture for task 03. .authoring/{design.md spoiler contract, notes-infra.md}. Authored this session by 9 parallel Sonnet subagents (8 tasks + capstone) against a live stack loaded at SCALE=0.01 (500k rows); GT temporarily set to 0.01 for verification then restored to 50M. All validators fail cleanly on stock stubs (NOT PASSED, exit 1, no traceback via guarded). Pass-paths proven live per task via throwaway reference impls written in place then reverted byte-identical (never committed) + all t0N_* ClickHouse objects dropped: t01 pruning (full sum(price) read 500000 rows vs pruned 8192 = one granule); t02 SummingMergeTree MV, 1440 (day,category) keys matched GT exactly after 5 incremental insert batches (7199 partial rows required FINAL/GROUP BY to collapse); t03 RMT+FINAL, 6000 raw rows -> 1915 distinct keys, survivors correct per key; t04 TTL DELETE 15 MONTH retained 231392/500000 (cutoff 2025-04-11), forcing genuinely required; t05/t07 correctness matched GT on both engines (t07 also cross-engine agreement), timing kept INFORMATIONAL not asserted (at 500k ClickHouse HTTP/per-query overhead makes it slower than Postgres and the DuckDB-vs-CH winner flips run-to-run — the real gap is a 50M-scale phenomenon, documented in the READMEs); t06 DuckDB-on-Parquet matched GT + pruned to one partition file; t08 writeup validator gates on filled ANSWER.md (5 sections, min 250 chars, no placeholder, grounding keywords); t09 CP1 rollup+business-questions matched GT (price_sum 36,780,355.61 at 0.01, top sellers, per_category_instock), CP2 DuckDB reproduced all independently + pruning, CP3 filled DESIGN.md + re-runs CP1/CP2 as subprocesses. Two ClickHouse gotchas found+baked into task 03 (and reused by capstone): (1) `optimize_on_insert` (session default 1) pre-collapses duplicate-key rows within a single insert block, silently defeating the dedup premise — insert with `settings={"optimize_on_insert": 0}`; (2) clickhouse-connect converts naive Python datetimes using the CLIENT machine's local tz, not the server's UTC — validators that insert naive datetimes set `os.environ.setdefault("TZ","UTC")` before first import. No reference solutions committed anywhere (grep-verified: only `raise NotImplementedError` + docstring guidance in every src/); ANSWER.md/DESIGN.md left as unfilled templates. Stock restored: GT back at 50M, ClickHouse `SHOW TABLES` = only observations_raw, no scratch/baseline-local.json/__pycache__ tracked. Stack left running — `docker compose down -v` for a cold stock state.
- [x] 10-nosql-patterns — done. 8 tasks (01-rate-limiter, 02-distributed-lock, 03-dedup-filter, 04-redis-streams-consumer, 05-mongodb-document-modeling, 06-mongodb-vs-jsonb, 07-nosql-decision-writeup, capstone 08-capstone with CP1 steady two-consumer materialization / CP2 chaos crash+XAUTOCLAIM convergence / CP3 DESIGN.md), module README, PROGRESS section, CONVENTIONS ports (SANDBOX_10_*: Redis Stack 6310, MongoDB 27310, Postgres 54310). Stack: redis/redis-stack-server:7.4.0-v3 (bundles RedisBloom BF.*, needed by task 03) + mongo:7 (root sandbox/sandbox, authSource=admin, db `sandbox`) + postgres:16 (task 06 JSONB side only), via docker-compose, all healthy in seconds. Data: generate.py two numpy streams (products seed 10101, events seed 10102) writes data/{products.json,events.json} NDJSON gitignored + data/ground-truth.json committed; SCALE=1.0 = 20000 products / 25000 events; events COUPLED to the catalog (each scrapes a real product, price = catalog price * exp(N(0,0.08)), 70% introduce a new product without replacement / 30% re-scrape a hot one => exactly 30% duplicate urls, 17500 unique); build_products/build_events are pure so validators can synth the corpus in-memory. Namespacing keeps the shared stack collision-free (Redis keys `s10:tNN:`, Mongo collections `tNN_`, Postgres schema `t06`) so validators run in parallel — never FLUSHALL. Shared harness/common.py (redis/mongo/pg client factories all lazy, guarded/passed/not_passed, redis_flush_prefix SCAN+DEL, run_concurrently). .authoring/{design.md spoiler contract, notes-infra.md wave-1 build, notes-live-verification.md this session}. Split across sessions: wave-1 built infra + generate.py (live-verified: 3 services healthy, RedisBloom BF.* probed, generate.py deterministic sha, GT numbers pinned) + authored all 8 tasks; THIS final session verified every task live via one parallel subagent each. All 8 stock stubs fail cleanly (single-line NOT PASSED, exit 1, no traceback — @guarded holds). Pass-paths proven live via throwaway reference impls written in place then reverted byte-identical (never committed): t01 rate limiter (50/500 admitted under concurrency, isolation, window reset; Lua EVAL INCR+PEXPIRE); t02 lock (mutual exclusion 120/120, wrong-owner reject, monotonic fences; SET NX PX + Lua compare-and-del + INCR fence); t03 dedup (SET exact 17500 vs Bloom 14 FP/0 FN at 1.6% memory; RedisBloom confirmed on the live image); t04 streams (2000 at-least-once, 500 stranded PEL reclaimed by 2nd consumer via XAUTOCLAIM); t05 Mongo modeling (correctness vs GT incl graded_query.count=2276, nested_query.count=2052, index-backed IXSCAN — validator explains raw filters so COLLSCAN can't pass); t06 Mongo-vs-JSONB (containment 2276 + nested black 2052 match on BOTH engines, PG uses gin(jsonb_path_ops) not Seq Scan, Mongo IXSCAN, partial update; 11 stubbed functions); t07 writeup (5 exact `##` sections >=200 chars, no `[fill in`, >=6/8 grounding keywords, NOTES.md >=150 chars); t08 capstone CP1 count=17500 price_sum=1700991.53 == GT, CP2 survived crash+XAUTOCLAIM reclaim (5000 reclaimed, XPENDING 0) still exact, CP3 DESIGN.md 5 sections + CP1/CP2 re-run green (ref: one doc per product_id in t08_state, per-batch max-(scraped_at,event_id) winner, watermark-guarded idempotent bulk_write). No bugs found (no wall-clock timeout gates a correct impl; CP2 timing is structural XAUTOCLAIM idle knobs, the 300/600s in validate.py are outer subprocess wrappers). This session also CREATED the missing 08-capstone/README.md, filled PROGRESS, bumped root README est-evenings 4->5–6, and fixed a stale "ten functions" -> "eleven" line in task 06's README. No reference solutions committed anywhere (grep-verified: NotImplementedError in every src/ stub, ANSWER.md/DESIGN.md unfilled templates); no scratch dirs, no *-local.json, no task-dir __pycache__ tracked (all gitignored). Stack left running — docker compose down -v for a cold stock state.
- [x] 11-python-concurrency — done. 9 tasks (01-event-loop-and-blocking, 02-taskgroup-structured-concurrency, 03-cancellation-and-timeouts, 04-backpressure-bounded-queue, 05-semaphore-rate-limiting, 06-gil-decision-matrix, 07-sync-async-bridging, 08-profiling-py-spy, capstone 09-capstone-async-scraper with CP1 steady / CP2 chaos / CP3 design memo), module README, PROGRESS section, root README already listed it (5-6 evenings). PURE PYTHON — no docker, no CONVENTIONS ports entry: `harness/peer.py` runs an in-process aiohttp mock server bound to 127.0.0.1:0 (ephemeral, no collisions), dial-in latency/jitter/max_concurrency/rate/error knobs + a PeerStats(total_requests, max_observed_concurrency, error_responses, throttled); the concurrency gate rejects BEFORE incrementing in_flight so max_observed_concurrency <= cap is a hard invariant. Data: generate.py seed 111111, pure numpy `build_corpus` -> 3000 pages `/p/{i}` -> {product_id, category, price} (Zipf categories, lognormal prices); data/corpus.json gitignored, data/ground-truth.json committed (sha ab2a3e98..5343: n_pages=3000, price_sum=324536.21, per_category_count electronics 1316 / home-goods 616 / kitchen 389 / toys 279 / sporting-goods 202 / apparel 198), regenerates deterministically. Shared harness/common.py (not_passed/passed/guarded/_last_line, time_it, write_baseline/read_baseline for machine-local *-local.json, load_ground_truth, run_async, snapshot_tasks/leaked_tasks, measure_peak_memory via tracemalloc, rss_bytes). Verification philosophy per design.md: structural/behavioral by default (leaked_tasks==[], max_observed_concurrency<=cap, aggregate vs ground truth with float tol on money); memory-bounded via tracemalloc PEAK RATIO not absolute bytes (task 04); relative-timing-only via baseline.py + read_baseline (tasks 06 GIL matrix + 08 profiling — the only two places the module makes speed claims, never an absolute wall-clock number). Toolchain uv (aiohttp, numpy, psutil, py-spy, pytest-asyncio). Generated across two waves: wave-1 built infra + harness + generate.py + tasks 01-05,07 (partial); THIS final wave generated 06/08/09 and completed 01/04/07 via 6 parallel Sonnet subagents, then a 7th serial subagent fixed+verified the peer/timing-sensitive tasks. ALL 11 validators fail cleanly on stock stubs (single-line NOT PASSED, exit 1, zero traceback lines — @guarded holds). Pass-paths proven live via throwaway reference impls in gitignored scratch/ then reverted byte-identical (never committed): t01 offloaded gather+to_thread (heartbeat frac ~1.0 vs inline ~0.54); t04 bounded Queue (peak-mem ratio 0.99x vs unbounded 3.92x, ~2.2x threshold discriminates); t05 semaphore+rate-limiter (throttled=0); t06 measured process 2.60x vs thread 0.94x for CPU-bound (GIL) + ~7.8x concurrent I/O, asserted margins have headroom; t07 bounded to_thread input-order-preserving (inline reference fails the responsiveness check); t08 py-spy attached cleanly on this Windows host (no elevation, caught compute_signature at top of active+gil stack), validator gates on behavioral heartbeat + ANSWER content (CONCURRENCY=1 to keep the fixed-vs-broken signal separated); t09 CP1 count=3000 price_sum=324536.21 max_observed_concurrency=24 == ground truth, CP2 converges to identical aggregate under error_rate=0.2+jitter with cap held/no leaks, CP3 gates DESIGN.md + re-runs CP1/CP2 (600s outer safety timeouts, never a correctness gate on wall-clock). TWO Windows/Proactor bugs found+fixed this wave (see .authoring/notes-live-verification.md): (A) mock_peer's IocpProactor.accept-loop Task false-flagged as leaked by any before/after-within-peer leak check — fixed VALIDATOR-side (no harness change) by snapshotting tasks BEFORE entering mock_peer and checking leaked_tasks AFTER teardown (spans peer lifetime); Linux/selector never hits this since accept is a callback not a Task, so prior modules missed it — applied to tasks 01/05, capstone already had it. (B) task 01's heartbeat check couldn't discriminate offloaded from inline because pure-Python CPU-bound work offloaded to threads still contends on the GIL (offloaded==inline ticks) AND the premise was false (that's task 06's lesson) — reframed the blocking cost to GIL-RELEASING (time.sleep stand-in = blocking I/O / native parser), recalibrated check 3 to this machine's achievable solo heartbeat rate (Windows ~64Hz floor makes elapsed/interval unreachable) asserting frac>=0.75; learner-facing "CPU-bound" wording retargeted to "blocking (I/O / GIL-releasing native)". Incidental: task 05 src/fetcher.py shipped a leftover broken full impl (raised ExceptionGroup) not a stub — restored to raise NotImplementedError. No reference solutions committed anywhere (grep-verified: raise NotImplementedError in every learner src/ except intentional non-stubs 06/src/workloads.py provided + 08/src/app.py the broken app to profile); ANSWER.md/DESIGN.md unfilled templates; no scratch/*-local.json/__pycache__ tracked. .authoring: design.md (spoiler contract), notes-infra.md (wave-1), notes-task08.md, notes-live-verification.md (this wave). Pre-existing unrelated working-tree edit `01-sql-foundations/03-currency-normalized-revenue/src/query.sql` left uncommitted (not part of this module).
- [x] 12-api-engineering — done. 10 tasks (01-pagination-offset-vs-cursor, 02-response-caching-redis, 03-rate-limiting-and-quotas, 04-background-jobs-and-idempotency, 05-streaming-large-exports, 06-sql-injection, 07-auth-jwt-and-refresh, 08-secrets-management, 09-load-test-and-bottleneck-hunt, capstone 10-capstone-catalog-api with CP1 steady / CP2 chaos-hardening / CP3 DESIGN.md), module README, PROGRESS section, CONVENTIONS ports (SANDBOX_12_PG_PORT 54312, SANDBOX_12_REDIS_PORT 6312), root README row corrected (est 5-6 -> 6-7; the old one-liner wrongly claimed "FastAPI on the module 02 DB"). OWN stack, NOT module 02's (design.md's "Own stack" section: module 02's DB is deliberately wrecked as ITS curriculum; reusing it would contaminate every pagination/caching benchmark here) — postgres:16 + redis:7 via docker-compose. No fixed API port anywhere: harness/service.py always binds 127.0.0.1:0, so parallel task/validator runs never collide. Data: generate.py seed 121212, one clean properly-indexed `shop` schema (2000 sellers / 60 categories / 200000 products / 20000 users / 500000 orders / 1188048 order_items, ~259MB, ~60s); data/ gitignored except committed ground-truth.json (sha d96d3ab6..a305, reproduced byte-identical this session); SCALE shrinks it, GROUND_TRUTH_ONLY=1 = fast answer-key path. Namespacing keeps the shared stack parallel-safe: `shop` is READ-ONLY (no task writes to it), writable state goes in per-task schemas tNN, Redis keys `s12:tNN:` flushed via redis_flush_prefix (NEVER FLUSHALL). Shared harness/{common,service,load}.py (guarded/passed/not_passed, pg/redis helpers, fixture-password scrypt contract, run_app/run_app_subprocess/asgi_client, bombard() asyncio load generator — no locust). .authoring/{design.md spoiler contract, notes-infra.md wave-1, notes-live-verification.md this wave}. Three waves: (1) infra+harness+generate.py; (2) tasks 01-06 authored, never committed, never reviewed; (3) THIS session restored/completed 01-06, authored 07-10 via 5 parallel Sonnet subagents, verified all live, wrote docs. ALL 12 validators fail cleanly on stock (exit 1, exactly ONE `NOT PASSED:` line, zero traceback leak). Pass-paths proven live via throwaway refs (scratch-*/ or in-place) then reverted byte-identical — none committed. TWO CONTAMINATION BUGS inherited from wave 2 and fixed this session: (1) task 04's src/app.py shipped a WORKING reference solution for get_or_create_job (the real stub was parked beside it in an untracked src/app.py.stub-backup) — a naive `grep -c NotImplementedError` missed it because the file held 4 stubs AND 1 solution; restore lesson: count stubs against the EXPECTED per-file number, not just nonzero. (2) task 06 shipped the FULLY SOLVED security task — both fix layers already applied (parametrized `ILIKE %s` + a `_search_dsn()` connecting as the least-privilege t06_search role the learner is meant to create), self-identifying as `# THROWAWAY REFERENCE FIX ... never committed`; caught by RUNNING tests/exploit.py, which reported `EXPLOIT FAILED: payload returned HTTP 500` — i.e. the security contract's "exploit must SUCCEED against stock" direction was broken. Restored to vulnerable stock (f-string interpolation + pg_dsn() admin conn); leftover t06_search ROLE dropped from live PG (schema t06 is legitimately created by validate.py's own setup, so it stays). Both were on-disk only — module 12 had never been committed, so git history was never contaminated. Security contract proven BOTH directions: 06's exploit leaks user4242@kupitron-mail.example + its scrypt hash out of shop.users via UNION against stock, and fails after the reference fix. Task 07: 9/9 traps rejected (alg=none, HS256/RS256 confusion, tampered sig, absent sig, expired, refresh-as-access, access-as-refresh, rotated-then-reused refresh, cross-user IDOR) + a NEGATIVE CONTROL (narrowing /me's catch to ExpiredSignatureError flipped 4/9 traps to VULNERABLE) proving the battery isn't rubber-stamping. Task 08: 6/6 planted secrets found incl. the history-only one, 0/4 decoys; fixture deterministic (identical manifests + commit shas across runs). Task 09: measured stock ~35 rps / p95 ~880-930ms vs full fix ~242-271 rps / p95 118-141ms (~7.0-7.6x); thresholds MIN_RPS_RATIO=4.5 / MAX_P95_RATIO=0.5 chosen FROM measurement so a full fix has headroom on slower machines but an N+1-only fix (4.1x / p95 0.68x) correctly FAILS; anti-cheat confirmed (hardcoded seller_name fails the oracle before any throughput check). Capstone: CP1/CP2/CP3 all PASSED against the reference, CP3 shown re-running CP1+CP2 as subprocesses; chaos drills proven non-vacuous (rate limiter admitted EXACTLY 10/50 then 10/100 concurrent — atomicity actually exercised; Redis-down asserts the specific X-Cache: BYPASS header, not a coincidental 200). Empirics in notes-live-verification.md: pyjwt 2.13.0 blocks PEM-shaped HMAC keys in encode() so traps.py hand-builds the confusion JWT; InvalidKeyError is a PyJWTError but NOT an InvalidTokenError; two access tokens minted in the same wall-clock second are legitimately byte-identical (assert the REFRESH token rotates, not the access token); harness redis_client()/pg_conn() sys.exit() on failure so app code needing graceful degradation must use its own low-level client; run_app_subprocess needs env PYTHONPATH on Windows and is the clean way to fake "Redis down" without stopping the shared container; Windows git marks loose objects read-only (rmtree needs onerror chmod); git commits are cumulative snapshots so task 08's fixture exposes valid_commits rather than one exact sha. Task 09's docstrings originally enumerated all three planted defects (would have handed over the hunt) — rewritten to plain code, mechanisms live only in the hint tiers. Task 07 commits a FIXTURE RSA keypair, clearly labeled "NEVER commit a real private key; this one signs nothing but fixture data" (same posture as fixture passwords; deliberate tension with task 08's lesson, resolved by labeling). Stub-handler style intentionally differs: 01/04/07/10 register NotImplementedError -> clean 501; 02/03/05 let it 500 and say so in their own READMEs. No reference solutions committed anywhere (grep-verified; only intentional non-stubs are 06's vulnerable app and 09's slow app); DESIGN.md + NOTES.md ship unfilled; no scratch/leaky-repo/*-local.json/__pycache__ tracked (.gitignore gained leaky-repo/ and 08-secrets-management/service/secrets/). Stack left running — docker compose down -v for a cold stock state. Pre-existing unrelated working-tree edit 01-sql-foundations/03-currency-normalized-revenue/src/query.sql predates this session, left untouched.
- [x] 13-scraping-at-scale — done. 7 tasks (01-hostile-target-recon, 02-data-quality-contracts, 03-change-detection-and-fingerprinting, 04-markup-resilience, 05-scraping-economics-budget-router, 06-observability-prometheus-grafana, capstone 07-capstone-data-quality-platform with CP1 steady day-0 / CP2 chaos-markup + change-detection + idempotent-resume / CP3 DESIGN.md 7 sections + CP1/CP2 subprocess re-run) + optional k8s-bonus (from-scratch `spider-platform` Helm chart: learner writes templates, values.yaml provided filled; validator does `helm lint` + `helm template` asserting a resource-bounded Deployment + probes + HPA + PDB — helm v3.18.2 present, kind/k3d NOT, so template/lint-only like modules 06/07). Module README (replaced the stub), PROGRESS section filled this session, CONVENTIONS ports (SANDBOX_13_TARGET_PORT 8313, SANDBOX_13_PROM_PORT 9313, SANDBOX_13_GRAFANA_PORT 3313), root README row already present (est 7-8). Stack: hostile FastAPI target (header/behavioral gate, honeypot traps -> instant ban, token-bucket rate limit capacity=25/refill=50/ban@25, 4 markup versions, per-day cumulative price/stock overlay + volatile per-request nonce, 6 bad-record defect types, /api/product/{id} as the paid headless-render stand-in @ 7x cost) + Prometheus + Grafana via docker-compose, all healthy. NO real browser/TLS (deliberate; JA3 is a planned second-wave module). Data: generate.py seed 131313, SCALE=1.0 = 4000 products; data/ gitignored except committed ground-truth.json (sha 335e94ca..80df0, UNCHANGED). Generation spanned prior sessions (wave-1 infra, then a task-authoring wave that authored 01-07 + k8s-bonus AND live-verified every pass-path via throwaway reference impls reverted byte-identical — documented in .authoring/notes-live-verification.md) but was interrupted before commit; THIS session did the final gate: re-confirmed no contamination (grep: only legit "throwaway client" hint text + generate.py comment; NotImplementedError stub counts per file as expected; k8s templates are TODO placeholders, values.yaml intentionally-filled), no junk tracked (dry-run add: no __pycache__/run/sinks/scratch/*-local.json/catalog.json/target-spec.json), ground-truth sha matches, then ran all 10 stock validators LIVE against the running stack — all fail cleanly (exit 1, single `NOT PASSED:` line, no traceback leak; 06 reports src/serve.py exited early, cp3/k8s report unfilled-template/empty-skeleton). Verification philosophy per design.md: recon/structural drives the target directly and compares to independently-computed ground truth (never trusts learner output as oracle); data-quality asserts exact bad-record id partition; change-detection has a mandatory nonce-strip + negative control; cost/budget gates on BOTH completeness>=0.98 AND modeled cost (gameable by always-render otherwise); observability is skip-if-down on Prometheus/Grafana with the learner's own /metrics content as the must-pass. One bug fixed in the authoring wave: 06/tests/validate.py's `_check_prometheus_live` called not_passed on up!=1 despite being documented skip-if-down (a crawl-then-serve solution legitimately reads up=0 at query time) -> now returns SKIPPED. No reference solutions committed anywhere; all deliverable docs (RECON.md/ANALYSIS.md/DESIGN.md/every NOTES.md) ship unfilled; dashboards/ holds only .gitkeep. Stack left running (target :8313 / Prometheus :9313 / Grafana :3313, ~14h uptime) — docker compose down -v for a cold stock state.
- [x] 14-stats-and-ml-foundations — done. 13 tasks across three arcs — Arc A (01-vectorization-and-broadcasting, 02-eda-scraped-prices, 03-matplotlib-fundamentals), Arc B applied-stats core (04-price-distributions-not-normal, 05-outliers-vs-parsing-errors, 06-confidence-intervals, 07-bootstrap, 08-ab-test-scraping-strategies, 09-correlation-vs-causation), Arc C shallow ML (10-sklearn-pipeline-leakage, 11-feature-engineering, 12-pytorch-tensors-autograd, capstone 13-capstone-text-classifier with CP1 classical baseline / CP2 PyTorch classifier / CP3 DESIGN.md + subprocess re-run) — module README, PROGRESS section, .authoring/design.md (spoiler contract). PURE-PYTHON, NO docker, NO CONVENTIONS ports entry (like module 11). Toolchain: `requires-python ">=3.12,<3.13"` HARD PIN — torch 2.13.0+cpu has no py3.14 wheel (the ambient interpreter) but resolves+installs+imports on 3.12.12 via `uv python`; deps numpy/pandas/polars/pyarrow/scipy/scikit-learn/matplotlib/torch(CPU)/pytest, committed uv.lock. Data: generate.py seed 141414, one shared "scraped product-price" dataset — `build_observations(seed,n)` pure builder returns (df, labels) where labels holds the hidden ground truth (defect_mask/defect_kind, non_usd_mask, genuine_outlier_mask, valid_mask, clean_price, confound tables) validators reconstruct in-memory; SCALE=1.0 = 60000 obs / 7968 products / 8 categories; data/observations.parquet gitignored, data/ground-truth.json committed (data_sha e2c7e74e..dc5a3, n_parsing_errors 2700 = 675 each negative/zero/missing_decimal/nan, n_non_usd 1138, valid_price_mean 114.89 / median 53.79 / p99 874.19 over the defect-free valid_mask, n_genuine_outliers 285). Dataset engineered for the arc: log-normal per-category prices (non-normal, task 04); planted price defects incl. missing_decimal=clean*100 that lands on whole dollars (task 05's second signal) vs genuine 99.5th-pct outliers; titles carry diluted category signal (generic-brand 30% + cross-category noun 25%) so a TF-IDF+LogReg title->category hits macro-F1 ~0.90, headroom for tasks 11/13; discount_pct/units_sold built as a Simpson's-paradox confound (pooled r~0.79, within-category ~0.01-0.06, task 09). Shared harness/common.py: not_passed/passed/guarded/_last_line, time_it/write_baseline/read_baseline (relative-timing baselines), load_ground_truth/load_observations, check_close/approx (money float-tolerance), require_figure (structural plot check — a Figure with >=N axes carrying drawn artists; visual correctness is human-checked per SPEC). Verification philosophy per design.md: stats tasks grade numeric answers vs an independent recompute within tolerance + require_figure; ML tasks use metric thresholds on a FIXED split seed (42, baked into scaffold+validator) so learner and validator grade the same held-out rows; the two timing tasks (01 vectorization speedup) use machine-local baselines only, never absolute wall-clock. Generation: one infra Sonnet subagent (built+verified generate.py determinism, harness, pyproject/uv.lock, design.md) then 13 parallel Sonnet task-authoring subagents. A mid-run usage-limit reset killed 8 authors partway (05/10/11/12 empty, 06/07/13 partial, 08 pre-restore); orchestrator cleaned junk (a shared scratch-explore/, a stray nested 14-.../14-.../ debris dir) and RESUMED each failed agent from its transcript via SendMessage (preserving their calibrated seeds/thresholds) to finish + verify + restore. ALL 15 validators (01-12 + capstone cp1/cp2/cp3) verified live on the final committed state: each fails cleanly on stock (exit 1, exactly one `NOT PASSED:` line, zero tracebacks; cp3 gates the unfilled DESIGN.md). Each author proved its pass-path live via a throwaway reference impl then reverted the stub byte-identical (never committed). Notable calibrations recorded by authors: t01 speedups 63-316x (thresholds 15x/40x/8x); t02 "observable valid" (price>0&USD&notNaN) INCLUDES the unobservable missing_decimal defects so its valid mean (246.84) differs from ground-truth's defect-free 114.89 — graded against an independent recompute, not the GT mean (median 54.75 robust); t04 raw skewness 34.1 -> log 1.02 (normaltest p underflows to 0 at this n on both, so log-is-more-normal keys on the skewness-ratio effect size); t05 reference hits missing_decimal recall 0.991 + 0 genuine-outlier FP via a whole-dollar + log-MAD rule while naive 3-sigma flags 7 real outliers and FAILS (trap proven non-trivial); t06 load_population() uses the true valid_mask so the CI is on clean electronics prices (mean 222.90, CI(200)=(174.54,246.33), width ratio 1.98~=sqrt(4)); t07 median bootstrap CI (44.00,58.81) brackets pop median 54.75; t08 z=5.79 p=7e-9 (unpooled-SE variant correctly rejected); t09 pooled r=0.793 max within |r|=0.071; t10 leaky R^2 0.526 vs honest 0.320 gap 0.206 (thresholds 0.10/0.15) — note product_id carries no true price signal so honest target-encoding is ~noise but the leak still reproduces cleanly; t11 base R^2 ~0 (swapped the confounded discount_pct out of the weak baseline for hour) vs engineered 0.433; t12 GD fit converges to closed-form OLS within 3e-7, autograd==finite-diff; t13 CP1 macro-F1 0.906 (thr 0.78) / CP2 0.898 (thr 0.80), CP3 subprocess re-run green. No reference solutions committed anywhere (grep-clean: only intended non-stub fixtures — 01/naive.py, 08/experiment.py, 10/split.py, 11/baseline.py, 13/data.py — carry real code; every learner deliverable is a NotImplementedError stub; ANSWER.md/DESIGN.md ship unfilled); no scratch/*-local.json/__pycache__ tracked (all gitignored); .venv ignored, uv.lock committed. Stock state clean and byte-consistent.
- [x] 15-llm-in-pipelines — done. 6 tasks (01-swappable-llm-client, 02-structured-extraction, 03-classification-and-enrichment, 04-embedding-dedup, 05-mini-rag, capstone 06-capstone with CP1 steady / CP2 chaos / CP3 DESIGN.md + subprocess re-run), module README, PROGRESS section, CONVENTIONS ports row (SANDBOX_15_OLLAMA_PORT 11439), root README row already present (est 4-5). Stack: single `ollama/ollama:latest` service via docker-compose, GPU reservation (nvidia count:all), host port 11439->11434, named volume `ollama-models`, `ollama list` healthcheck; NOT auto-pull — learner runs `docker compose exec ollama ollama pull qwen2.5:7b-instruct nomic-embed-text` once (documented in README). GENERATION-TIME MODEL DECISION (verified live on this RTX 3070 Ti 8GB): `qwen2.5:7b-instruct` (4.7GB, chat/JSON extraction) + `nomic-embed-text` (0.3GB, 768-dim embeddings) run 100% on GPU SIMULTANEOUSLY (~5GB total < 8GB), ~0.5s warm gen latency; JSON mode + structured-output schema pass-through both work. Swappable-client requirement honored: `harness/llm.py` = raw-httpx transport (no ollama/openai SDK dep) with `OllamaClient` + `OpenAIClient` + `get_client()` factory reading `LLM_PROVIDER`/`LLM_MODEL`/`LLM_EMBED_MODEL`/`LLM_BASE_URL`/`LLM_API_KEY`/`LLM_TIMEOUT` — one env var swaps local Ollama for a cloud OpenAI-compatible API, zero code change; task 01 is the learner building a resilience WRAPPER (reask-on-invalid-JSON, retry+backoff, primary->fallback, token/latency accounting) OVER that transport, tested with injected fake `LLMClient` subclasses for the deterministic retry/reask/fallback paths + one live smoke (fast + deterministic, not gated on model content). pyproject: `requires-python >=3.12,<3.14`, deps httpx/numpy/pytest, uv.lock committed. Data: generate.py SEED=151515, pure builders (build_catalog canonical source-of-truth + build_extraction_set 50 hostile-HTML snippets across 6 templates / build_classification_set 80 diluted-signal records / build_dedup_set 55 items in 20 gold clusters via 4 title distortions / build_rag_corpus 6 fixed synthetic "Sandbox Handbook" docs + 15 QA); data/ gitignored except committed ground-truth.json (data_sha ad73e0688ccfe83753a57564cc2361bda8ebd9f056578d83fbb11ebde0645e44); validators reconstruct gold in-memory from the build_* functions (module-14 df/labels split pattern), never trusting the gold-STRIPPED on-disk data/{extraction,classification,dedup}.json or learner output as oracle. RAG-corpus deviation (synthetic handbook, not live repo docs) is deliberate+documented in design.md: pointing RAG at live CONVENTIONS.md would drift the QA gold on every repo edit. Shared harness/common.py: module-11/14 pass/fail plumbing (not_passed/passed/guarded/_last_line/load_ground_truth/write_baseline/read_baseline) + `require_client()` (probes /api/tags, checks both models present, NOT PASSED with the exact `docker compose up`+pull commands on failure — the load-bearing infra-not-ready vs wrong-solution distinction, called FIRST in every LLM validator) + metrics prf_from_sets/accuracy/macro_f1/pair_f1 + norm_price/norm_text/cosine. Verification philosophy (design.md): metric thresholds calibrated LIVE against qwen2.5:7b-instruct at temperature=0 with GENEROUS headroom (7B sampling variance + CUDA nondeterminism => greedy decode isn't bit-stable); constant/degenerate baselines must FAIL each threshold (proven per task). Generated by 1 infra subagent (Sonnet, verified compose config + uv sync + generate.py determinism + live transport smoke + require_client) then 6 parallel Sonnet task-authoring subagents, each calibrating thresholds live and proving its pass-path via a throwaway reference reverted byte-identical (never committed). Orchestrator FINAL live gate this session: all 8 stock validators (01-05 + capstone CP1/CP2/CP3) fail cleanly from module root (exit 1, single NOT PASSED line, zero traceback; CP3 rejects the unfilled DESIGN.md); docker-compose actually boots (smoke on alt port 11440, serves /api/tags, GPU reservation accepted, torn down); git dry-run shows only ground-truth.json tracked under data/ and zero junk (no pycache/scratch/*-local.json/.venv/.npy); every learner src/ is a NotImplementedError stub (grep-verified: 01 client.py 3 NIE + given TransientError/StructuredOutputError, 02 extract 1, 03 classify 1, 04 dedup 1, 05 rag 3, capstone pipeline 4 + explain 2; all `client.*` mentions are docstring guidance, no leaked logic), all NOTES.md/DESIGN.md ship as unfilled templates. Measured reference metrics vs chosen thresholds (headroom): t02 extraction name/brand/currency/in_stock=1.00, price=0.96 (thr 0.85/0.85/0.85/0.85/0.80); t03 category macro_f1=0.68 (thr 0.42, constant-majority baseline 0.037 fails), brand_acc=0.99 (thr 0.75); t04 pair_f1=1.00 (thr 0.75; intra-cluster cosine min 0.885 / inter-cluster max 0.858 clean gap; all-singleton f1=0.0 + all-one-cluster f1=0.071 both fail); t05 hit@3=1.00 (thr 0.80; first-k baseline 0.20 / random-k 0.47 fail), answer-fact=0.867 (thr 0.55); capstone CP1 all stages pass (extraction 1.00/0.96, class macro_f1=0.62/brand 1.00, dedup pair_f1=1.00, quarantine 0.00, explain hit 1.00/answer 0.80), CP2 chaos (18/50 HTML truncated + 17/130 calls junked) catalog_precision=0.75/quarantine_recall=0.54 (thr 0.65/0.35; a no-op gate scored 0.585 so CP2 precision threshold raised to 0.65 to reject it), CP3 DESIGN.md gate + subprocess re-run green. Both my out-of-band verification Ollama container (`s15-ollama-test` on 11439, holds the pulled models in volume `sandbox15_ollama`) and the models remain — the committed compose uses its own `ollama-models` volume, so a learner (or a cold verify) does a one-time pull. Stop the test container with `docker rm -f s15-ollama-test` if a fully cold state is wanted (models persist in the `sandbox15_ollama` volume; `docker volume rm sandbox15_ollama` to reclaim the ~5GB).
- [x] 16-testing-engineering — done. 7 tasks (01-property-based-parsing, 02-stateful-and-metamorphic, 03-integration-postgres-testcontainers, 04-integration-redis-testcontainers, 05-contract-tests-api, 06-mutation-testing-taste, capstone 07-capstone-scrape-to-serve-test-suite with CP1 unit+property / CP2 integration+contract / CP3 DESIGN.md + subprocess re-run), module README (replaced stub), PROGRESS section + CONVENTIONS note (no fixed-port row — testcontainers uses ephemeral random ports, no module docker-compose.yml) already in place, root README row corrected (est 3-4 -> 5-6, one-liner now names the inversion). THE INVERSION: unlike every other module, the learner does NOT write an implementation — the sandbox ships a GIVEN correct `src/impl.py` (committed in the open, intentionally readable) and the learner writes the TEST SUITE (`tests/test_*.py`, the deliverable); grading is MUTANT-KILLING via `harness/mutation.py::grade()`, two gates: (1) green-on-correct (suite must fully pass the correct impl AND collect >= min_tests — rejects empty/`assert False`/collection-error), (2) kills every mutant in `.authoring/mutants/NN/*.py` (a mutant the suite still passes against SURVIVED; survivors named by opaque id `m03` only, bug lives in the mutant file's top docstring so the failure line is not a spoiler). Learner tests import `from src.sut import ...` — a generated shim (`write_sut_shim`) that re-execs the file at `SUT_IMPL_PATH` (the mutant) or falls back to `from . import impl`; each mutant graded in a FRESH `[sys.executable, -m, pytest]` subprocess (no import-cache contamination; `sys.executable` not bare `python` is load-bearing on Windows). Toolchain: uv, `requires-python >=3.11`, deps hypothesis/pytest/testcontainers/psycopg[binary]/redis/fastapi/httpx/jsonschema/numpy/cosmic-ray (uv.lock committed). `mutmut` was tried and REMOVED (refuses to run on native Windows, issue #397); `cosmic-ray>=8.4.6` is the real mutation tool for task 06 and works on Windows PROVIDED the test-command uses `sys.executable` (a bare `python` resolves to a different interpreter without pytest and silently reports every mutant "killed" with empty output — documented in .authoring/design.md). No module-level docker-compose: tasks 03/04/07 use testcontainers (`postgres:16`/`redis:7`, same tags as module 12, both cached) on random host ports; tasks 01/02/05/06 need no containers. This session was the FINAL live-verification gate (prior session authored everything but never committed — module was fully untracked, state still said pending, no notes-live-verification.md existed). Orchestrator proved the engine end-to-end on task 01 personally (stock fails cleanly; a reference Hypothesis suite killed 6/6; stub restored byte-identical) then delegated tasks 02-07 to 6 parallel Sonnet subagents, each: confirm stock fails cleanly (single `NOT PASSED:` line, exit 1, no traceback) -> write a throwaway reference suite proving the pass-path -> revert the stub byte-identical. ALL results: t01 killed 6/6, t02 6/6, t03 7/7 (real Postgres: idempotent upsert/watermark >-vs->=/keyset pagination/id tiebreak), t04 7/7 (real Redis: TTL/atomic check-then-set/namespace isolation/rate-limit boundary/dedup NX+EX), t05 8/8 (jsonschema contract + pagination edge polarity + type drift + cache header), t06 stock 63 survivors -> strengthened suite 0 survivors across 205 cosmic-ray mutants (baseline passes = genuine survivors not a broken test-command; notable non-obvious survivors chased: Mul_Div on the keyword-only `*` marker killed only by a `pytest.raises(TypeError)` positional call, NumberReplacer on `round(...,2)` needing a genuine 3rd-decimal value, tier-boundary n-1 needing a strictly-between value), t07 CP1 7/7 + CP2 9/9 + CP3 (DESIGN.md content gate + CP1/CP2 subprocess re-run green). No equivalent mutants anywhere (every mutant killable — confirmed by all agents; design.md notes equivalent mutants were excluded during authoring, e.g. the toy-proof clamp `x<lo`->`x<=lo` silent-equivalent rewritten to change a returned value, and cosmic-ray `core/*_Is` identity-operator equivalents to watch on small ints — none surfaced this run). NO contamination: every committed learner test file is a genuine stub with 0 test functions EXCEPT task 06's `tests/test_target.py` which is the intentional GIVEN weak-but-green suite (4 tests, the thing the learner strengthens); all NOTES.md/DESIGN.md ship as unfilled templates (DESIGN.md 5 `[fill in]` markers); `src/impl.py` / `src/sut.py` / mutant banks never edited. One learner-facing FIX this session: task 05's hint-1 + test scaffold recommended a synchronous `httpx.Client(transport=httpx.ASGITransport(...))` which RAISES `AttributeError: ... no attribute 'handle_request'` on httpx 0.28.1 (ASGITransport is async-only) — rewritten to lead with `fastapi.testclient.TestClient` (sync) and note the raw-httpx route needs `httpx.AsyncClient`; stub still fails cleanly after the edit. Git dry-run verified clean (130 files, no __pycache__/.venv/.pytest_cache/*.sqlite/cr.toml/*-local.json tracked — task 06's validate.py runs cosmic-ray entirely in a `tempfile.TemporaryDirectory()` so no session db/config leaks). No scratch dirs, no reference solutions committed anywhere. Docker left as-is (testcontainers self-reap via Ryuk; only pre-existing containers from other modules remain).
- [x] 17-system-design — done. 6 tasks (01-price-monitoring-10k-sites, 02-price-history-storage, 03-delivery-with-client-slas, 04-multi-tenant-platform, 05-outage-postmortem-redesign, capstone 06-capstone-design-review with CP1 requirements+capacity / CP2 architecture+failure+3 ADRs / CP3 hostile defence + risk register + REVIEW.md + CP1/CP2 subprocess re-run), module README (replaced stub), PROGRESS section, root README row corrected (5-6 -> 6-7, one-liner now names the two gates), CONVENTIONS exception row (writing module: no ports, no docker-compose). PURE PYTHON, NO docker, NO services, NO ports — `pyproject.toml` (requires-python >=3.11, pytest only) + committed uv.lock. THE MODULE'S CENTRAL PROBLEM was making a writing module objectively gradable; solved with TWO GATES per task: (1) CAPACITY GATE — the learner writes a back-of-the-envelope model as pure functions in `src/estimate.py` over a committed `workload.json`, and the validator recomputes every value INDEPENDENTLY from the same pinned formula (never importing learner code) across the shipped workload PLUS >=2 perturbed variants built in memory, so an implementation returning constants FAILS (proven per task; task 04 additionally asserts >=2 qualitatively different unsatisfied-tenant sets across variants so a memorized fair-share allocation cannot pass); (2) DOCUMENT GATE — `check_sections` (required `## ` headings, per-section min_chars, no `[fill in` placeholders), `check_keywords` grounding vocabulary, `check_quantitative` (the design must make numeric claims), `check_answers` (`### Q1`..`### Qn` hostile-review answers). Because the capacity model is graded numerically, each task README carries a "Capacity model contract" pinning every unit, rounding rule and derived quantity so exactly ONE correct number exists per function. Shared `harness/common.py` (module-14/16 pass/fail plumbing: not_passed/passed/guarded/_last_line, plus load_workload/check_close/import_estimate/check_estimate_module and the doc-gate checks). `.authoring/{design.md spoiler contract, notes-live-verification.md}`. Generated by 1 infra subagent then 6 parallel Sonnet task-authoring subagents, each proving its own pass path live with a throwaway reference reverted byte-identical (never committed). Task 05 is INVERTED: it ships a given `INCIDENT.md` (timestamped timeline + evidence only, zero analysis, root cause deducible not stated — slow degraded 200s -> late parse exceptions -> a retry policy whose `backoff.base_ms: 0` makes its "exponential" backoff numerically inert -> ~2.4x retry amplification -> queue-depth-only autoscaler doubles the fleet -> shared connection pool oversubscribed -> an UNRELATED delivery-api sharing that pool fails -> DB-dependent liveness probes restart-loop -> manual drain at minute 130), and the learner reconstructs and quantifies it. TWO REAL BUGS FOUND AND FIXED IN THE SHARED HARNESS by the orchestrator this session (three authors hit the first independently and had worked around it locally by raising min_chars): (1) `check_answers`' verbatim-copy detection compared the answer body only against its own FIRST PHYSICAL LINE, so a multi-line question pasted as the answer slipped through whenever the question text alone cleared min_chars — fixed by comparing against the real question source (`questions_path=`, whitespace-normalized, subtracting difflib matching runs >=40 chars so re-wrapping does not disguise a copy) and requiring `min_original_chars` (default 120) of the learner's own content; all six validators now pass `questions_path`; (2) that same heuristic FALSE-FAILED a legitimate single-paragraph answer (`stripped == lines[0]` is true for any one-line body) — task 02's template does not restate the question, so a correct one-paragraph answer would have been rejected; the heuristic now runs only as a fallback when `questions_path` is absent. Hardening: `_original_char_count` de-duplicates repeated sentences first, otherwise padding a copied question with the same filler sentence N times reads as N sentences of original work (this bypass was measured working on tasks 01 and 06 before the fix). Both directions re-verified by the orchestrator across all six tasks on their REAL `DESIGN.md` templates and `HOSTILE-REVIEW.md` files: a copied+padded question is rejected 6/6, a genuine answer is accepted 6/6. Capstone's aggregate `tests/validate.py` also collapsed from a two-line failure to the single-line convention. FINAL LIVE GATE (orchestrator, on the committed state): all 9 validator entry points fail cleanly — exit 1, exactly one `NOT PASSED:` line, zero traceback lines. No reference solutions committed anywhere (grep-verified: 7/9/9/8/9/12 `raise NotImplementedError` stubs in the six `src/estimate.py`; every DESIGN.md, NOTES.md, REVIEW.md and the three `docs/adr-00N.md` ship as unfilled templates); no `__pycache__`, scratch dirs or `*-local.json` tracked. Nothing to leave running — the module has no services.
- [x] 18-rust-track — done. 8 tasks + optional bonus (01-log-parser-aggregations, 02-toy-expression-interpreter, 03-csv-to-parquet, 04-url-health-checker, 05-bitcask-kv-store, 06-tui-log-dashboard, 07-async-fetch-pipeline, capstone 08-capstone-price-watch with CP1 ingest+bitcask / CP2 parquet-export+freshness / CP3 concurrency-cap+crash-recovery+DESIGN.md, bonus proc-macro-bonus t09), module README (replaced stub), PROGRESS section filled this session, CONVENTIONS exception row added (Cargo workspace: no docker-compose/pyproject/host-port; `cargo test -p <pkg>` is the validator). NOT a Python/Docker module: a Cargo workspace (edition 2024, resolver 3, rust-version 1.95, Cargo.lock committed) at 18-rust-track/Cargo.toml replaces all of it; deps pinned once in [workspace.dependencies] (serde/arrow+parquet 59.1.0/ratatui 0.30.2+crossterm 0.29/tokio 1.53/tempfile/syn+quote+proc-macro2), members write dep.workspace = true. GRADING CONTRACT (design.md, the module's CONVENTIONS exception): each task graded by GIVEN cargo integration tests under its own tests/ dir; a failing/panicking assertion exits non-zero and prints its explanatory message (every assert carries a "why" message) — this module's documented stand-in for the repo-wide NOT PASSED/exit-1 rule. Shared crate `sandbox18-harness` (harness/src/): ground_truth.rs (GroundTruth/LogGroundTruth/CsvGroundTruth loaders), fixture_server.rs + async_fixture_server.rs (FixtureServer bind 127.0.0.1:0 ephemeral, RouteConfig with status/body/with_delay/fail_first(n)=drop-TCP-connection, ServerStats.max_concurrency atomic counter — the structural concurrency oracle), prng.rs (Xorshift64), tempdir.rs. Data: `sandbox18-datagen` seed 0xC0FFEE_5EED writes data/access.log (~200k Combined-Log-Format-ish lines + appended response-time-ms, Zipf paths, ~1.5% corrupted) and data/products.csv (~500k rows, 12 Zipf categories, ~2% dirty) plus data/ground-truth.json accumulated over pre-corruption records (independent answer key, all BTreeMap for stable bytes); SCALE env var default 1.0; regenerated byte-identical this session (ground-truth sha 478fe29b..b681da, access.log 27482506 / products.csv 30421286 bytes). THIS SESSION was the final verification gate (prior sessions authored all tasks + harness + datagen but never committed; state said pending, no notes-live-verification.md existed). Orchestrator built the workspace clean (only stub warnings), verified data determinism, confirmed all 9 stock stubs fail cleanly, then delegated one throwaway-reference-impl pass-path proof per task to 9 parallel subagents. ALL pass-paths proven live then stubs restored byte-identical (orchestrator re-checked every sha against the committed tree): t01 29 tests (parse 9/hand-built 12/ground-truth 8 over the 27MB corpus), t02 62 (incl a 900-tree property suite; note IP-octet corruption edge — only status 200 can hide in an octet since octets <=256), t03 20 (parse 11/fixture 3/ground-truth 6, arrow RecordBatch->SNAPPY parquet, set_max_row_group_row_count), t04 11 (std threads only: thread::scope + Arc<Mutex<VecDeque>> + mpsc + hand-rolled HTTP/1.1 client; Windows connect_timeout-to-refused surfaces as TimedOut not ConnectionRefused so all connect errors are retryable), t05 13 (bitcask: FNV-1a framed records, keydir replay distinguishing clean-EOF vs torn header/record/checksum then set_len-truncate, compact drops append handle before rename for Windows), t06 30 (pure App seam via handle_event + TestBackend cell assertions, ratatui 0.30 API), t07 13 (tokio JoinSet + Semaphore held across fetch-then-send, watch-channel shutdown, retry-by-count against fail_first, timeout never wall-clock), t08 capstone cp1 2/cp2 1/cp3 4 (async ingest->parse->bitcask->parquet; cp3 concurrency structural max_concurrency==3 for CAP 3, crash offsets reachable, DESIGN.md filled to pass then restored to unfilled template sha f2c2b667..057257), t09 12 (#[derive(Builder)] via syn/quote; Option<Inner>=optional field, others required, missing-field error in declaration order). proc-macro EXCEPTION: t09 stock stub fails at COMPILE time ("proc-macro derive panicked" + todo! help, cascading E0599 no method builder, cargo exit 101) — the only clean-fail possible for a stubbed derive, documented in its README, accepted. No test/harness bugs found (crash-injection offsets reachable, concurrency structural, no wall-clock gates). No reference solutions committed anywhere (grep-clean: all 9 src/lib.rs retain todo!/unimplemented! + signatures + guidance doc-comments only); all NOTES.md unfilled, capstone DESIGN.md unfilled template. hints/hint-3.md for task 02 carries near-complete pseudocode but is explicitly labelled non-copy-pasteable (last-resort tier, consistent with the hint-3 definition) — kept. No junk tracked (git dry-run 122 clean files under module; no .bak/scratch/target/access.log/products.csv; the two large data files gitignored, only ground-truth.json committed). .authoring/{design.md, notes-live-verification.md this session}. Nothing to leave running — no services.
- [x] 19-ts-track — done. 3 tasks (01-type-challenges `@sandbox19/t01` — 12-challenge type-level progression: MyPick, DeepReadonly, TupleToUnion, First/Last, EventByTag+assertNever, Getters, PartialBy, ReplaceReturnType, MyAwaited, ExtractParams, Flatten, Brand; 02-type-safe-sdk-client `@sandbox19/t02` — zod schemas + z.infer types, generic validated `request` core, cursor pagination + async iterator, auth with rotating refresh + auto-refresh-on-401, malformed routes must throw; capstone 03-capstone-monorepo-contracts `@t3/{contracts,api,worker,web,e2e}` with CP1 contracts+api agree / CP2 worker job round-trip + exhaustiveness anti-drift / CP3 web anti-cast e2e + DESIGN.md gate), module README (was already written by the infra wave), PROGRESS section + CONVENTIONS exception row filled this session, root README row corrected (3–4 -> 6–9, one-liner now names the three tasks). NOT a Python/Docker module: a pnpm workspace (root package.json + pnpm-workspace.yaml + committed pnpm-lock.yaml; typescript 7.0.2, zod 4.4.3, vitest 4.1.10, @types/node 26.1.1, tsx 4.23.1; strict tsconfig.base.json incl. noUncheckedIndexedAccess/exactOptionalPropertyTypes/verbatimModuleSyntax) replaces docker-compose/pyproject/ports — the module's documented CONVENTIONS exception. GRADING CONTRACT: `pnpm --filter <pkg> run typecheck` (tsc --noEmit) and/or `run test` (vitest run); type errors surface as clean TS2344 at `Expect<Equal<...>>` lines (harness `Equal` uses the HKT trick so `any` fails; `@ts-expect-error` reject-cases catch over-permissive stubs), test failures carry descriptive messages — the module's stand-in for NOT PASSED/exit-1. Shared given `@sandbox19/harness` (packages/harness): type-testing.ts (Expect/Equal/NotEqual/IsAny/IsNever/IsUnknown/Alike), mock-server.ts (deterministic marketplace API over node:http, seed 0xc0ffee mulberry32, 200 products/8 categories/12 sellers, keyset pagination with opaque base64url cursors, auth login/rotating refresh/me, deliberately-malformed routes /products/malformed + /products/wrongshape, binds 127.0.0.1:0 ephemeral), report.ts. Infra wave (prior session) built workspace+harness+lockfile+module README+design.md; THIS session smoke-tested the mock server live (all endpoint families incl. malformed + auth), authored all 3 tasks via 3 parallel Sonnet subagents, and ran the final gate. Stock-failure stories (all verified live this session on final state): t01 typecheck exit 1 (~68 clean TS2344/TS2578 lines) + test exit 1 (assertNever/Brand runtime suites), t02 typecheck exit 0 + test exit 1 (18/18 fail with clean `not implemented`, server always closed, no unhandled-rejection spew), t03 contracts/api/worker/web typecheck green on stubs + e2e typecheck exit 1 (TS2344 at hand-pinned assertions) + cp1/cp2/cp3 fail cleanly (23/24 `not implemented` + DESIGN.md-placeholder message). Pass-paths proven live by each author via throwaway reference impls written in place, all validators fully green (t01 typecheck 0 + 5/5 tests, t02 18/18 twice for determinism, t03 all 5 typechecks + 24/24 twice), then reverted byte-identical (sha256 verified). Anti-cheat proven: t01 `any`/hardcoded-single-instantiation answers rejected (Equal distinguishes any; note: `Alike` lets `any` pass, so t01/07 uses per-property Equal + assignability checks — recorded in notes-t01.md); t02 negative control — cast-only `return json as T` client fails exactly the 2 malformed-route tests while 16 happy-path tests pass; t03 drift proofs — renaming Product.price in contracts alone breaks e2e typecheck + cp1 runtime, adding a job kind alone breaks worker typecheck at assertNever (the exhaustiveness lever), web cast-only swap fails cp3's anti-cast test. Checkpoint command gotcha: `pnpm --filter @t3/e2e run test cp1` (NO `--`; the `-- cp1` form does not filter under pnpm 10 + vitest 4 — documented in capstone README + notes-t03.md). Zod resolves from the hoisted root node_modules in all task packages (verified; no per-package dep added, lockfile untouched). t02 schema stubs are `z.custom<Shape>(() => true)` so z.infer matches the target shape and stock typecheck stays green while validating nothing — replacing them with real z.object schemas is the task. No reference solutions committed anywhere (grep-verified: 12 t01 stubs `= unknown`, t02 client.ts 12x `throw new Error("not implemented")` + zero fetch/parse logic in any stub, t3 handlers/jobs/client all stubs); hints are direction->mechanism->pseudocode only; DESIGN.md + all NOTES.md ship as unfilled templates; no junk tracked (git dry-run 97 files, no node_modules/dist/tsbuildinfo/scratch/probe files; stray _probe.mjs debris from the infra session deleted). .authoring/{design.md spoiler contract (infra wave), notes-t01.md, notes-t02.md, notes-t03.md (this session)}. Nothing to leave running — no services.
- [x] 20-kubernetes — done. 22 tasks across 6 arcs + shared infra, against a long-lived local kind cluster `sandbox20` (3 nodes: 1 control-plane + 2 workers, Calico v3.29.1 for REAL NetworkPolicy enforcement — kindnet doesn't enforce, which task 14 depends on). Infra (built earlier sessions, live-verified): `app/app.py` (stdlib ThreadingHTTPServer, every broken state expressible via env knobs — START/READY_DELAY_S, FAIL_READY, FAIL_HEALTH_AFTER_S, CRASH_ON_START/CRASH_AFTER_S, MEM_MB/LEAK_MB_PER_S, CPU_BURN_THREADS, REQUIRED_ENV, TERM_IGNORE, WORK_MODE server|consumer|producer with QUEUE_BACKEND redis|rabbitmq; endpoints /,/healthz,/readyz,/work,/env,/metrics; graceful SIGTERM drain), images `sandbox20-app:1.0/2.0/distroless` + `redis:t11-repack` all built and `kind load`ed by `scripts/build-images.sh` (redis repack = single-platform `docker build --provenance=false --sbom=false FROM redis:7-alpine, because the multi-platform manifest+attestation can't `kind load` into containerd on this host — fix added THIS session to build-images.sh + design.md so task 11's queue is reproducible on a fresh clone), `cluster/kind-config.yaml` (apiServerPort 6320, ingress host ports 8320/9320 on the `ingress-ready=true` control-plane), `harness/common.py` (kubectl/kubectl_json, require_cluster asserting 3 Ready nodes + calico-node DS, ensure_ns/delete_ns, wait_until/wait_rollout, Windows-safe port_forward, http_get, guarded/not_passed/passed single-line + exit 1, doc-gate check_sections/check_keywords/check_answers reused from module 17), `pyproject.toml`+`uv.lock` (pyyaml/requests/kopf/kubernetes/pytest), `scripts/cluster-up.sh` (installs Calico pinned v3.29.1) `/cluster-down.sh` `/build-images.sh`. CONVENTIONS ports added (6320/8320/9320, SANDBOX_20_*); PROGRESS.md 20-kubernetes section filled with the 22-task ladder + capstone checkpoints; root README row already present (est 12-15). `.authoring/design.md` = the full spoiler contract (topology, knob table, per-task verification approach, tNN namespacing rule, `s20-tNN/` node-mutation prefix rule, cluster-global-install ownership table) + `.authoring/notes-t01..t22.md`. Namespacing: task NN uses namespace `tNN`, never touches another's; only task 20 may cordon/drain nodes. Cluster-global installs each OWNED by one task with a committed re-runnable install.sh(+uninstall.sh), left installed for later tasks: metrics-server (08), ingress-nginx controller-v1.12.1 (13 — pinned after v1.13.0's kind manifest dropped the `ingress-ready` nodeSelector and silently bound host ports nowhere), CloudNativePG 1.29.2 (15), Argo CD v3.4.5 + in-cluster Gitea 1.24.7-rootless (16 — repo `http://gitea-http.argocd.svc:3000/sandbox20/platform-charts.git`), RabbitMQ 3.13-management + Prometheus v2.55.1 + prometheus-adapter v0.12.0 (19). Generation split across sessions: prior sessions built infra + Arc 1-2 (01-07, verified in-session then, re-scanned clean this session — all learner stubs are TODO skeletons, spot-checked 01/03/04/05 stock-fail live this session); THIS multi-session run authored+verified Arc 3 (08-11), Arc 4 (12-15), Arc 5 (16-18), Arc 6 (19-22) via parallel Sonnet subagents (each: read design.md+harness+exemplars, author full task dir, prove stock fails cleanly with exactly one `NOT PASSED:` line + exit 1 + no traceback, prove the pass-path live with a throwaway reference reverted byte-identical via sha256, clean up its tNN namespace + any `s20-tNN/` taints, write notes-tNN.md, return only a short summary). Task 20 (pdb-vs-node-drains) authored INLINE by the orchestrator after its subagent hit a session limit mid-write. ALL 22 tasks verified: every stock validator fails cleanly; every reference pass-path proven live (throwaway solutions reverted byte-identical; NO reference solution committed anywhere — grep-clean: every learner src/ YAML is a `# TODO(you)` skeleton, t22 operator.py raises NotImplementedError, all ANALYSIS/DESIGN/INCIDENT/MAPPING/COMPARISON/NOTES ship as unfilled `[fill in]` templates). Per-task empirics worth keeping (in notes-tNN.md): 08 OOMKill on containerd 2.0.3 reports terminated.reason `Error` not `OOMKilled` so the gate keys on exit 137; 09 pod-d anti-cheat must keep the worker2 nodeSelector + add a toleration (not drop the selector); 10 ephemeral-container evidence checked on a dedicated never-rolled `render-debug-target` Pod (the fix rolls the real pod, erasing its ephemeralContainers); 11 hidden root cause = ConfigMap key typo `QUEUE_KEY`->`QUEUE_KEE` via envFrom cascading to loud CrashLoops (api/worker REQUIRED_ENV) + a silent unconsumed-queue symptom (producer); 12 headless-Service misuse bypasses kube-proxy so a generic resolve+curl probe catches all three defects; 14 NetworkPolicy needs the UDP+TCP 53 DNS-egress leg or even allowed legs fail, negative legs proven blocked by Calico (self-heal-of-drift non-vacuousness proven by a selfHeal:false control); 15 CNPG failover via forced primary-pod delete, keyed on status.readyInstances/currentPrimary; 16 Gitea rootful image CrashLoops under a non-root podSecurityContext (s6 PID1 needs root) -> `-rootless` tag; 17 PreSync hook needs a hook-delete-policy or a self-heal re-sync deadlocks recreating the completed Job; 18 `git revert`'s default message quotes the reverted subject verbatim so the "still-unreverted?" check matches on `startswith(marker)` not substring; 19 prometheus-adapter needs `resources.overrides.namespace` + `return_per_object_metrics` and MSYS_NO_PATHCONV=1 for bare `/apis/...` kubectl args, full HPA scale-up->down observed; 20 `kubectl drain --pod-selector app=web` evicts only the fleet (leaving the platform untouched) + a soft topologySpread so the drain doesn't deadlock + finally-uncordon-every-node, PDB desiredHealthy gate rejects too-weak AND too-strict budgets; 22 kopf operator run as a subprocess by the validator (Windows: no --context flag so pin via KUBECONFIG env, terminate() is the only stop since kopf ignores OS signals on Windows). No junk tracked (root .gitignore covers .venv/__pycache__/scratch/*-local.json; git dry-run 328 files, no .tgz/Chart.lock/vendored charts/kubeconfig). Cluster left RUNNING with all cluster-global installs in place (metrics-server, ingress-nginx, CNPG operator, Argo CD+Gitea, RabbitMQ+Prometheus+adapter); `scripts/cluster-down.sh` (or `kind delete cluster --name sandbox20`) for a cold teardown, then `scripts/cluster-up.sh`+`scripts/build-images.sh`+each owning install.sh to rebuild.
- [x] toolkit (t1–t4) — done. 4 non-Docker, no-host-port tool-fluency sub-modules, 21 tasks total, no capstones. Each ships pyproject.toml + committed uv.lock + module README.md + `harness/common.py` (module-17 pass/fail + doc-gate plumbing) + `.authoring/{design.md,notes-live-verification.md,PROGRESS-section.md}`. CONVENTIONS.md gained a toolkit exception paragraph; root README gained a "Toolkit track" table (dip-in-anytime, t1/01 early); PROGRESS.md gained a "# Toolkit track" section with all 21 tasks. Tools provisioned this session (persist on the machine, needed for live verification): scoop ripgrep 15.2 / fd 10.4 / duckdb CLI 1.5.5 / hyperfine 1.20; GNU `parallel` 20260722 standalone perl script at ~/.local/bin/parallel; `uv tool install` pyright 1.1.411 + pre-commit 4.x; jq 1.8, ruff, mypy, uv 0.10.9, git 2.48, node, claude already present. Root .gitignore extended with `**/.ruff_cache/`, `**/.mypy_cache/`, `**/work/`.
  - **t1-ai-assisted-engineering** (6 tasks): 01-project-memory (CLAUDE.md doc-gate for a given priceparser sample lib), 02-custom-subagents (YAML-frontmatter-parsed test-runner/code-reviewer agents + checklist + WHEN-NOT-TO-DELEGATE doc-gate), 03-hooks-and-guardrails (settings.json structural check + real subprocess exercise of the hook scripts against pass/fail + ruff-clean/dirty fixtures), 04-headless-and-ci (`claude -p` script + label-triggered GH Actions workflow, structural), 05-mcp-server (FULLY BEHAVIORAL: validator does a real MCP stdio handshake via the official SDK client against the learner's FastMCP server), 06-verification-discipline (module-16-style inversion: 4 patches — async race / pagination off-by-one / silent bool() coercion + one clean control — learner writes REVIEW.md verdicts + tests_learner/test_patchNN.py; validator checks verdicts vs hardcoded ground truth AND runs each learner test against the shipped buggy code). Deps: pytest, mcp==1.28.1, pyyaml. Note: task 05 fixture dir is `fixture/` not `data/` (root `**/data/` would have gitignored it); async-race patch reproducibility empirically confirmed 5/5.
  - **t2-modern-python-toolchain** (5 tasks): 01-uv-project-management (fix broken pyproject: missing dep + console entry point + dev group; `uv sync`/`uv run`/`uv tool run` all pass), 02-ruff-lint-and-format (configure select I/B + line-length + scoped per-file-ignores, fix real violations, zero-noqa cap, over-broad-ignore cheat rejected), 03-typing-strict (`mypy --strict` + 4 genuine typing bugs incl. a live AttributeError and a silent None return; given pytest keeps behavior honest), 04-pre-commit-wiring (write .pre-commit-config.yaml wiring ruff+ruff-format+strict-mypy+two hygiene hooks; clean fixture passes / bad fixture fails in throwaway git repos), 05-packaging-internal-library (fix broken pyproject: hatchling backend + version + console_scripts; `uv build` -> wheel+sdist, installed into throwaway venv, console script runs, real src layout). Dev tools pinned: ruff>=0.15, mypy>=1.7, pre-commit>=4. Fixed a harness bug: shutil.rmtree left read-only .git on Windows -> onerror chmod+retry.
  - **t3-cli-data-toolkit** (5 tasks): 01-jq-nested-json (flatten/join/group/reshape nested scraped JSON), 02-ripgrep-and-fd (regex capture group / fd glob exclusion / lookaround / extension census), 03-duckdb-cli-swiss-knife (hive-partitioned aggregate + CSV join + window fn via the duckdb CLI), 04-hyperfine-benchmark (honest RELATIVE micro-benchmark of two counting approaches with --warmup + --export-json; validator asserts stated winner matches JSON, never an absolute time), 05-gnu-parallel-batch (fan a per-file jq transform across 30 inputs, --jobs>=2 + joblog). generate.py deterministic (sha bb74178c…5b20e9a, 102 files / 301KB at SCALE=1.0), data/ gitignored (validators recompute ground truth in Python). Gotchas baked in: prefer `shutil.which("bash")` over WSL System32 bash.exe stub; duckdb CLI needs `pwd -W` (rejects /d/ POSIX paths); hyperfine shells via cmd.exe so globs need double quotes; parallel's first-run notice is stderr-only.
  - **t4-git-advanced** (5 tasks): 01-interactive-rebase-cleanup (messy 8-commit -> 5-commit linear via rebase -i --autosquash --root, validator checks final tree equals intended content), 02-bisect-find-regression (14-commit history, first-bad at commit 9; `git bisect run`; learner writes SHA to FIRST_BAD_SHA.txt stub, validator recomputes independently), 03-worktrees-parallel (two `git worktree` branches, checked via branch end-state + `git worktree list --porcelain`), 04-reflog-rescue (recover a force-deleted branch's exact original SHA via reflog), 05-history-design-writeup (POLICY.md doc-gate + 6-question HOSTILE-REVIEW). Determinism: every setup.sh pins GIT_AUTHOR/COMMITTER dates + core.autocrlf false + core.eol lf + `.gitattributes (* -text)` as first commit, so hardcoded-SHA validators are stable on Windows Git; per-task scratch repos live under gitignored work/.
  - Verification: EVERY one of the 21 tasks live-verified by its authoring subagent (stock validator fails cleanly with single-line NOT PASSED + exit 1; throwaway reference solution passes; reference reverted byte-identical / scratch reset). Orchestrator independently spot-checked t4/05 + t1/04 stock (both clean NOT PASSED, exit 1). git add -n sweep: 231 files, no .venv/__pycache__/.ruff_cache/.mypy_cache/work/data/-local.json/reference-solution leakage. No reference solutions committed anywhere; all deliverables (CLAUDE.md, POLICY.md, REVIEW.md, ANSWER.md, NOTES.md, solve.sh/setup outputs) ship as unfilled stubs/templates or broken-config-to-fix. Committed + pushed to main.
- [ ] ci-meta — pending (stub README only)

Rules: one module at a time, in this order. Update this file after every module. Skeleton (README.md, PROGRESS.md) is generated first, before module 01.
