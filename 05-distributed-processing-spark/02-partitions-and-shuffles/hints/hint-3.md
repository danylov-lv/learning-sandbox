For `repartition_vs_coalesce`, the shape is: at the top of the function, disable AQE. Read `jsonl_dir`, filter out rows where `_corrupt_record` is not null, and immediately call `.rdd.getNumPartitions()` on that filtered DataFrame — this is your `source_partitions`. Then build `repartitioned = filtered.repartition(target_partitions_repartition)` and `coalesced = filtered.coalesce(target_partitions_coalesce)` as two independent derived DataFrames off the same `filtered` source (don't chain one off the other). For each, capture `harness.common.get_plan(df)` and `.rdd.getNumPartitions()`, and assemble the five-key return dict the docstring specifies. You don't need to call an action on either derived DataFrame — the plan and the partition count are both available without materializing anything.

For `shuffle_partitions_effect`, loop over `configured_values`, and inside the loop: AQE off, `spark.conf.set("spark.sql.shuffle.partitions", value)`, then read-and-filter `jsonl_dir` fresh (or reuse a cached filtered read from outside the loop — the config only affects what happens *after* it's set, so re-reading isn't required, but the `groupBy` has to happen after the `set` call for that iteration's value to take effect), run `.groupBy("source_id").agg(...)` — count or sum, either satisfies the contract since only partition count is checked — and record `result.rdd.getNumPartitions()` under `str(value)` in the output dict. The important ordering detail: set the config, *then* build and the groupBy DataFrame; building it before the `set()` call bakes in whatever value was configured previously.

For `skew_partition_counts`, the shape has four stages. First, setup: AQE off, read `jsonl_dir`, filter `_corrupt_record` nulls, then `.distinct()` on the whole row to collapse retry-storm duplicates — keep this deduplicated DataFrame around, both variants and the final ground-truth comparison are built from it, not from the raw read. Second, naive: `deduped.repartition(n_partitions, "source_id")`, then attach `F.spark_partition_id()` as a column and `groupBy` it, `count()`, collect to the driver, and turn that into a plain Python list of length `n_partitions` indexed by partition id (fill in zero for any index the collected result didn't include — a `groupBy(spark_partition_id())` only returns rows for partitions that got at least one row, and a heavily-skewed naive run is exactly where you'd expect some partitions to come up empty). Third, salted: add a salt column (an int in `[0, n_salts)`, e.g. `(F.rand() * n_salts).cast("int")`, or a `pmod` of a hash of a per-row-unique column against `n_salts` if you want it deterministic instead of random), build a composite key column (e.g. `F.concat(col("source_id"), F.lit("_"), col("salt"))`), `.repartition(n_partitions, salt_key_col)`, and measure per-partition counts the same way as the naive variant, producing another length-`n_partitions` list. Fourth, de-salt and verify: from the salted, salt-keyed DataFrame (the one with both `source_id` and `salt` columns still present, before or after the repartition — the aggregation below doesn't care about physical partitioning), `groupBy("source_id").count()` (or `.agg(F.count("*"))`), collect, and build the `rows_by_source_after_dedup` dict with `str(source_id)` keys and plain-int values — this is a straight aggregate over `deduped`'s rows regardless of the salt column, so it should equal `ground-truth.json`'s `rows_by_source` if your dedup step matched task 01's. Return all three pieces under the exact keys the docstring lists.
