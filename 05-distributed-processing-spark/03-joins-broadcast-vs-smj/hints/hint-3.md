For `broadcast_enrich`: read and dedup the events first (drop `_corrupt_record`, then whole-row `distinct()`), keep the result in a variable. Read both reference CSVs. Join events to the sources DataFrame wrapped in `broadcast(...)`, on `source_id`; take that result and join it to the categories DataFrame wrapped in `broadcast(...)`, on `category_id`. Set AQE off before building any of this. Capture the plan on the twice-joined DataFrame. For the two grouped counts, `groupBy("region").count()` and `groupBy("vertical").count()` on that same joined DataFrame, collected into plain dicts keyed by the group value.

For `force_sort_merge`: set both configs (`adaptive.enabled=false`, `autoBroadcastJoinThreshold=-1`) at the top. Read events, filter `_corrupt_record` null and `http_status == 200`, add a `month` column via `substring("captured_at", 1, 7)`. Build two aggregate DataFrames filtered to `month == "2025-11"` and `month == "2025-12"` respectively, each `groupBy("product_id", "source_id")` with `avg("price")` and `count("*")`. Join them on the two shared key columns — plain `.join(other, ["product_id", "source_id"])`. That's your SMJ-forced result; capture its plan and row count. Build the same join again but wrap one side in `broadcast(...)` before joining, and compare row counts — they must match.

For `aqe_converts_join`: set `adaptive.enabled=true`, leave the broadcast threshold untouched. Same event read/filter/month setup as above, but aggregate by `product_id` alone (drop `source_id` from the group), for the same two months. Build the joined DataFrame on `product_id`. Call `get_plan(joined, "formatted")` right away — nothing has executed yet, this is your "before" capture. Then call `joined.collect()` (small enough at this aggregation level to be fine), and call `get_plan(joined, "formatted")` again on the exact same `joined` variable — this is your "after" capture, and it should differ from the "before" one specifically in whether the join shown is sort-merge or broadcast, and in the `isFinalPlan` marker's value.
