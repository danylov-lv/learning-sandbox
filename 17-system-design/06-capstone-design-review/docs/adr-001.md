# ADR-001: Acquisition fan-out and backpressure transport

## Context

[fill in: the acquisition fleet has to fan work out to thousands of
fetch workers against sites with wildly different rate limits, and fan
results back in to the quality/contract layer, while giving the fleet a
way to apply backpressure when a downstream stage is slow. You have run
Scrapy over RabbitMQ in production for two years — that experience is
relevant context here, not just a footnote. State the forces at play:
throughput, ordering (or lack of it) requirements, backpressure/replay
needs, operational familiarity, and how this choice interacts with the
multi-tenancy story.]

## Decision

[fill in: the transport and fan-out mechanism you are choosing for this
platform, and the one or two sentence reason it wins for THIS workload —
not a generic "it's popular" reason.]

## Alternatives considered

[fill in at least two rejected alternatives as bullets, each with the
specific reason it lost — a real trade-off (cost, operational burden,
missing feature, latency characteristic), not "it was worse."]

- [fill in: alternative 1 and why it was rejected]
- [fill in: alternative 2 and why it was rejected]

## Consequences

[fill in: what this decision costs you going forward — operational
burden, a feature you gave up, a migration you are now committed to if
this choice turns out wrong, coupling it creates elsewhere in the design.]
