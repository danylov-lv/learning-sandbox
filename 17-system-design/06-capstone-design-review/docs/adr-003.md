# ADR-003: Multi-tenant isolation model

## Context

[fill in: hundreds of tenants (`clients.tenant_count`) with unequal
weight and unequal SLA tiers share the same acquisition fleet and storage.
State the forces: noisy-neighbor risk from a tenant that requests a large
`records_per_delivery` at high `deliveries_per_day`, the cost of full
per-tenant dedication vs. a shared pool, and how strict the isolation
needs to be given the SLA gap between the `enterprise` and `self_serve`
tiers in `workload.json`.]

## Decision

[fill in: the isolation model you are choosing (e.g. shared fleet with
per-tenant quotas, tenant-partitioned storage, dedicated capacity for the
top SLA tier, or some mix) and why it matches the tenant-weight skew in
this workload.]

## Alternatives considered

[fill in at least two rejected alternatives as bullets, each with the
specific reason it lost.]

- [fill in: alternative 1 and why it was rejected]
- [fill in: alternative 2 and why it was rejected]

## Consequences

[fill in: what this decision costs — spare capacity you must carry,
complexity in the quota/enforcement path, what happens to a tenant at
the boundary of their quota, and how this interacts with the degradation
ladder under overload.]
