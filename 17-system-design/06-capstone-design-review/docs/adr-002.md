# ADR-002: Five-year historical storage and analytical serving engine

## Context

[fill in: five years of price history has to support both a hot,
low-latency read path (recent prices, per-tenant delivery) and an
analytical serving layer (`analytics.queries_per_day` against
`analytics.avg_bytes_scanned_per_query`) over the full retention window.
State the forces: write volume from `daily_new_rows`, the hot/cold split
from `storage.hot_tier_window_days`, the query pattern the analytical
layer needs to serve well, and the compression assumptions already baked
into `workload.json`.]

## Decision

[fill in: the storage/serving engine(s) you are choosing for hot vs. cold
tiers, and why this shape serves both the delivery path and the
analytical path without one starving the other.]

## Alternatives considered

[fill in at least two rejected alternatives as bullets, each with the
specific reason it lost.]

- [fill in: alternative 1 and why it was rejected]
- [fill in: alternative 2 and why it was rejected]

## Consequences

[fill in: operational cost of running this engine (or combination of
engines), what migration looks like if the query pattern changes (this
connects directly to Q10 in the hostile review), and any consistency or
freshness trade-off this creates between the hot and cold tiers.]
