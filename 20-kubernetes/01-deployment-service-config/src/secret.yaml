# TODO(you): write a Secret here.
#
# Required shape:
#   apiVersion: v1
#   kind: Secret
#   metadata:
#     name: <your choice>
#   type: Opaque
#   stringData:            # (or `data:` with base64-encoded values)
#     <key of your choice>: <token value of your choice>
#
# The key you pick here is the one deployment.yaml's APP_SECRET_TOKEN env
# var must reference via secretKeyRef. See README.md "What's required".
