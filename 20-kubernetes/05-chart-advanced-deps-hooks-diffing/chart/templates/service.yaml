# TODO(you): write the worker Service here.
#
# Required shape:
#   apiVersion: v1
#   kind: Service
#   metadata:
#     name: worker
#   spec:
#     type: ClusterIP
#     selector:
#       app: worker
#     ports:
#       - port: 80
#         targetPort: 8080
#
# This is what the validator port-forwards to in order to hit /metrics.
