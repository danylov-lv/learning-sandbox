apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "t18-workload.fullname" . }}
  labels:
    {{- include "t18-workload.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "t18-workload.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "t18-workload.labels" . | nindent 8 }}
    spec:
      containers:
        - name: sandbox20-app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
