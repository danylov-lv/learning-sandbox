# TODO(you): corrected pod-c.
#
# Same Pod name (pod-c). Diagnose the real problem from
# `kubectl describe pod pod-c` / `kubectl get events` / `kubectl get pod
# pod-c -o yaml` first -- see README.md and DIAGNOSIS.md. Once you know the
# root cause, write a working Pod here (same name, same container image)
# that actually schedules and reaches Running/Ready.
