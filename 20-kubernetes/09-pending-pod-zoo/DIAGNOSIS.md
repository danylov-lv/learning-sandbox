# Diagnosis

For each pod: quote the exact symptom you saw in `kubectl describe pod` /
`kubectl get events` output, state the root cause in your own words, and
explain why your fix in `fixes/<name>.yaml` addresses it. See README.md for
what's graded. Write these from your own terminal output, not from memory
of the shape of a typical Kubernetes problem -- the validator checks these
sections are grounded in actual scheduler vocabulary (`FailedScheduling`,
`Insufficient`, `affinity`, `taint`, `toleration`, `unbound`, `storage
class`, ...), not generic prose.

## pod-a

[fill in]

## pod-b

[fill in]

## pod-c

[fill in]

## pod-d

[fill in]

## pod-e

[fill in]
