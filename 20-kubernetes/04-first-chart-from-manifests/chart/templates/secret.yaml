# TODO(you): the worker Secret -- rendered ONLY when .Values.secret.enabled
# is true (wrap the whole resource in a Go-template "if" action on that
# value, closed with the matching "end" action).
#
# - name via the fullname helper, labels via the labels helper.
# - type: Opaque.
# - one data (or stringData) key holding .Values.secret.token -- the
#   Deployment's APP_SECRET_TOKEN env var reads this key via secretKeyRef,
#   also only when secret.enabled is true.
